---
title: "在傳統入口網站中從 Azure 容錯回復 VMware VM | Microsoft Docs"
description: "了解在將 VMware VM 和實體伺服器容錯移轉至 Azure 後，如何將其容錯回復到內部部署網站。"
services: site-recovery
documentationcenter: 
author: ruturaj
manager: mkjain
editor: 
ms.assetid: 7ca86e21-7a5d-45ab-8f4b-e42da90f199a
ms.service: site-recovery
ms.devlang: na
ms.tgt_pltfrm: na
ms.topic: article
ms.workload: storage-backup-recovery
ms.date: 06/05/2017
ms.author: ruturajd
ms.openlocfilehash: 82d5eb7fd13b1e9700a3e9bc2d30775e9c129749
ms.sourcegitcommit: 02e69c4a9d17645633357fe3d46677c2ff22c85a
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 08/03/2017
---
# <a name="fail-back-vmware-virtual-machines-and-physical-servers-to-the-on-premises-site-classic-portal"></a><span data-ttu-id="74421-103">將 VMWare 虛擬機器和實體伺服器容錯回復至內部部署網站 (傳統入口網站)</span><span class="sxs-lookup"><span data-stu-id="74421-103">Fail back VMware virtual machines and physical servers to the on-premises site (classic portal)</span></span>
> [!div class="op_single_selector"]
> * [<span data-ttu-id="74421-104">Azure 入口網站</span><span class="sxs-lookup"><span data-stu-id="74421-104">Azure Portal</span></span>](site-recovery-failback-azure-to-vmware.md)
> * [<span data-ttu-id="74421-105">Azure 傳統入口網站</span><span class="sxs-lookup"><span data-stu-id="74421-105">Azure Classic Portal</span></span>](site-recovery-failback-azure-to-vmware-classic.md)
> * [<span data-ttu-id="74421-106">Azure 傳統入口網站 (舊版)</span><span class="sxs-lookup"><span data-stu-id="74421-106">Azure Classic Portal (Legacy)</span></span>](site-recovery-failback-azure-to-vmware-classic-legacy.md)
>
>

<span data-ttu-id="74421-107">本文說明如何將 Azure 虛擬機器從 Azure 容錯回復至內部部署網站。</span><span class="sxs-lookup"><span data-stu-id="74421-107">This articles describes how to fail back Azure virtual machines from Azure to the on-premises site.</span></span> <span data-ttu-id="74421-108">當您準備好使用本 [教學課程](site-recovery-vmware-to-azure-classic.md)，將已從內部部署網站容錯移轉至 Azure 的 VMware 虛擬機器或 Windows/Linux 實體伺服器容錯回復時，請遵循本文中的指示。</span><span class="sxs-lookup"><span data-stu-id="74421-108">Follow the instructions in this article when you're ready to fail back your VMware virtual machines or Windows/Linux physical servers after they've failed over from the on-premises site to Azure using this [tutorial](site-recovery-vmware-to-azure-classic.md).</span></span>

## <a name="overview"></a><span data-ttu-id="74421-109">Overview</span><span class="sxs-lookup"><span data-stu-id="74421-109">Overview</span></span>
<span data-ttu-id="74421-110">下圖說明此案例的容錯回復架構。</span><span class="sxs-lookup"><span data-stu-id="74421-110">This diagram shows the failback architecture for this scenario.</span></span>

<span data-ttu-id="74421-111">當處理序伺服器是內部部署，而且您使用 ExpressRoute，請使用此架構。</span><span class="sxs-lookup"><span data-stu-id="74421-111">Use this architecture when the process server is on-premises and you are using an ExpressRoute.</span></span>

![](./media/site-recovery-failback-azure-to-vmware-classic/architecture.png)

<span data-ttu-id="74421-112">當處理序伺服器在 Azure 上，而且您有 VPN 或 ExpressRoute 連接，請使用此架構。</span><span class="sxs-lookup"><span data-stu-id="74421-112">Use this architecture when the process server is on Azure and you have either a VPN or an ExpressRoute connection.</span></span>

![](./media/site-recovery-failback-azure-to-vmware-classic/architecture2.png)

<span data-ttu-id="74421-113">若要查看完整的連接埠清單及容錯回復架構圖，請參閱下圖</span><span class="sxs-lookup"><span data-stu-id="74421-113">To see the complete list of ports and the failback architechture diagram refer to the image below</span></span>

![](./media/site-recovery-failback-azure-to-vmware-classic/Failover-Failback.png)

<span data-ttu-id="74421-114">容錯回復的運作方式如下：</span><span class="sxs-lookup"><span data-stu-id="74421-114">Here’s how failback works:</span></span>

* <span data-ttu-id="74421-115">在容錯移轉至 Azure 之後，您可以透過幾個階段來容錯回復至內部部署網站：</span><span class="sxs-lookup"><span data-stu-id="74421-115">After you’ve failed over to Azure, you fail back to your on-premises site in a few stages:</span></span>
  * <span data-ttu-id="74421-116">**階段 1**：重新保護 Azure VM，使其開始複寫回到在內部部署網站執行的 VMware VM。</span><span class="sxs-lookup"><span data-stu-id="74421-116">**Stage 1**: You reprotect the Azure VMs so that they start replicating back to VMware VMs running in your on-premises site.</span></span> <span data-ttu-id="74421-117">啟用重新保護時，VM 會移入最初在建立容錯移轉保護群組時所自動建立的容錯回復保護群組。</span><span class="sxs-lookup"><span data-stu-id="74421-117">Enabling reprotection moves the VM into a failback protection group that was created automatically when you originally created the failover protection group.</span></span> <span data-ttu-id="74421-118">如果您已將容錯移轉保護群組新增至復原計畫，則也會自動將容錯回復保護群組新增至該計畫。</span><span class="sxs-lookup"><span data-stu-id="74421-118">If you added your failover protection group to a recovery plan then the failback protection group was also automatically added to the plan.</span></span>  <span data-ttu-id="74421-119">在重新保護期間，您將指定如何規劃容錯回復。</span><span class="sxs-lookup"><span data-stu-id="74421-119">During reprotect you specify how to plan to fail back.</span></span>
  * <span data-ttu-id="74421-120">**階段 2**：Azure VM 複寫到內部部署網站後，就要執行容錯移轉以從 Azure 容錯回復。</span><span class="sxs-lookup"><span data-stu-id="74421-120">**Stage 2**: After your Azure VMs are replicating to your on-premises site, you run a fail over to fail back from Azure.</span></span>
  * <span data-ttu-id="74421-121">**階段 3**：容錯回復資料之後，您必須重新保護已容錯回復到的內部部署 VM，使其開始複寫至 Azure。</span><span class="sxs-lookup"><span data-stu-id="74421-121">**Stage 3**: After your data has failed back, you reprotect the on-premises VMs that you failed back to, so that they start replicating to Azure.</span></span>


  [!VIDEO https://channel9.msdn.com/Blogs/Azure/Enhanced-VMware-to-Azure-Failback/player]

### <a name="failback-to-the-original-or-alternate-location"></a><span data-ttu-id="74421-122">容錯回復到原始位置或替代位置</span><span class="sxs-lookup"><span data-stu-id="74421-122">Failback to the original or alternate location</span></span>
<span data-ttu-id="74421-123">如果您已容錯移轉 VMware VM，在其仍存在於內部部署環境的前提下，您可以將其容錯回復到相同的來源 VM。</span><span class="sxs-lookup"><span data-stu-id="74421-123">If you failed over a VMware VM you can fail back to the same source VM if it still exists on-premises.</span></span> <span data-ttu-id="74421-124">在此案例中，只會容錯回復差異變更。</span><span class="sxs-lookup"><span data-stu-id="74421-124">In this scenario only the delta changes will be failed back.</span></span> <span data-ttu-id="74421-125">請注意：</span><span class="sxs-lookup"><span data-stu-id="74421-125">Note that:</span></span>

* <span data-ttu-id="74421-126">如果您容錯移轉實體伺服器，則一律會容錯回復至新的 VMware VM。</span><span class="sxs-lookup"><span data-stu-id="74421-126">If you failed over physical servers then failback is always to a new VMware VM.</span></span>
  * <span data-ttu-id="74421-127">在容錯回復實體機器之前，請注意：</span><span class="sxs-lookup"><span data-stu-id="74421-127">Before failing back a Physical machine note that</span></span>
  * <span data-ttu-id="74421-128">當從 Azure 容錯移轉回復至 VMware 時，受保護的實體機器會回復成虛擬機器。</span><span class="sxs-lookup"><span data-stu-id="74421-128">Physical machine protected will come back as a Virtual machine when failed over back from Azure to VMware</span></span>
  * <span data-ttu-id="74421-129">請確認連同您需要容錯回復的必要 ESX/ESXi 主機，您探索至少一部主要目標伺服器。</span><span class="sxs-lookup"><span data-stu-id="74421-129">Ensure that you discover atleast one Master Target sever along with the necessary ESX/ESXi hosts to which you need to failback.</span></span>
* <span data-ttu-id="74421-130">如果您要容錯回復到原始 VM，則必須符合下列條件：</span><span class="sxs-lookup"><span data-stu-id="74421-130">If you fail back to the original VM the following is required:</span></span>

  * <span data-ttu-id="74421-131">如果 VM 是由 vCenter 伺服器管理，主要目標的 ESX 主機應該可以存取 VM 資料存放區。</span><span class="sxs-lookup"><span data-stu-id="74421-131">If the VM is managed by a vCenter server then the Master Target's ESX host should have access to the VMs datastore.</span></span>
  * <span data-ttu-id="74421-132">如果 VM 位於 ESX 主機上，但不受 vCenter 管理，則 VM 的硬碟必須位於可由主要目標主機存取的資料存放區內。</span><span class="sxs-lookup"><span data-stu-id="74421-132">If the VM is on an ESX host but is not managed by vCenter then the hard disk of the VM must be in a datastore accessible by the MT's host.</span></span>
  * <span data-ttu-id="74421-133">如果 VM 位於 ESX 主機上，但未使用 vCenter，則應該先完成主要目標 ESX 主機的探索後，才能重新保護。</span><span class="sxs-lookup"><span data-stu-id="74421-133">If your VM is on an ESX host and doesn't use vCenter then you should complete discovery of the ESX host of the MT before you reprotect.</span></span> <span data-ttu-id="74421-134">如果您要容錯回復實體伺服器，則也適用這一條件。</span><span class="sxs-lookup"><span data-stu-id="74421-134">This applies if you're failing back physical servers too.</span></span>
  * <span data-ttu-id="74421-135">另一個選項 (如果內部部署 VM 存在) 是先將其刪除，然後才執行容錯回復。</span><span class="sxs-lookup"><span data-stu-id="74421-135">Another option (if the on-premises VM exists) is to delete it before you do a failback.</span></span> <span data-ttu-id="74421-136">如此一來，容錯回復便會在與主要目標 ESX 主機相同的主機上建立新的 VM。</span><span class="sxs-lookup"><span data-stu-id="74421-136">Then failback will then create a new VM on the same host as the master target ESX host.</span></span>
* <span data-ttu-id="74421-137">當您容錯回復至替代位置時，資料將會復原到內部部署主要目標伺服器所使用的相同資料存放區和相同 ESX 主機上。</span><span class="sxs-lookup"><span data-stu-id="74421-137">When you failback to an alternate location the data will be recovered to the same datastore and the same ESX host as that used by the on-premises master target server.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="74421-138">必要條件</span><span class="sxs-lookup"><span data-stu-id="74421-138">Prerequisites</span></span>
* <span data-ttu-id="74421-139">您需要擁有 VMware 環境才能容錯回復 VMware VM 和實體伺服器。</span><span class="sxs-lookup"><span data-stu-id="74421-139">You'll need a VMware environment in order to fail back VMware VMs and physical servers.</span></span> <span data-ttu-id="74421-140">不支援容錯回復至實體伺服器。</span><span class="sxs-lookup"><span data-stu-id="74421-140">Failing back to a physical server isn’t supported.</span></span>
* <span data-ttu-id="74421-141">若要容錯回復，您應該在最初設定保護時就已建立 Azure 網路。</span><span class="sxs-lookup"><span data-stu-id="74421-141">In order to fail back you should have created an Azure network when you initially set up protection.</span></span> <span data-ttu-id="74421-142">要進行容錯回復，就需要有從 Azure 網路 (Azure VM 所在網路) 連往內部部署網站的 VPN 或 ExpressRoute 連線。</span><span class="sxs-lookup"><span data-stu-id="74421-142">Failback needs a VPN or ExpressRoute connection from the Azure network in which the Azure VMs are located to the on-premises site.</span></span>
* <span data-ttu-id="74421-143">如果您想要容錯回復到的目標 VM 是由 vCenter 伺服器進行管理，您必須確定已擁有在 vCenter 伺服器上探索 VM 的必要權限。</span><span class="sxs-lookup"><span data-stu-id="74421-143">If the VMs you want to fail back to are managed by a vCenter server you'll need to make sure you have the required permissions for discovery of VMs on vCenter servers.</span></span> <span data-ttu-id="74421-144">[閱讀更多資訊](site-recovery-vmware-to-azure-classic.md)。</span><span class="sxs-lookup"><span data-stu-id="74421-144">[Read more](site-recovery-vmware-to-azure-classic.md).</span></span>
* <span data-ttu-id="74421-145">如果 VM 上有快照，重新保護程序將會失敗。</span><span class="sxs-lookup"><span data-stu-id="74421-145">If snapshots are present on a VM then reprotection will fail.</span></span> <span data-ttu-id="74421-146">您可以刪除快照或磁碟。</span><span class="sxs-lookup"><span data-stu-id="74421-146">You can delete the snapshots or the disks.</span></span>
* <span data-ttu-id="74421-147">在容錯回復之前，您需要先建立幾個元件：</span><span class="sxs-lookup"><span data-stu-id="74421-147">Before you fail back you’ll need to create a number of components:</span></span>
  * <span data-ttu-id="74421-148">**在 Azure 中建立處理序伺服器**。</span><span class="sxs-lookup"><span data-stu-id="74421-148">**Create a process server in Azure**.</span></span> <span data-ttu-id="74421-149">在容錯回復期間，您必須建立這個 Azure VM 並使其保持運作。</span><span class="sxs-lookup"><span data-stu-id="74421-149">This is an Azure VM that you’ll need to create and keep running during failback.</span></span> <span data-ttu-id="74421-150">容錯回復完成後，便可以刪除該機器。</span><span class="sxs-lookup"><span data-stu-id="74421-150">You can delete the machine after failback is complete.</span></span>
  * <span data-ttu-id="74421-151">**建立主要目標伺服器**：主要目標伺服器會傳送及接收容錯回復資料。</span><span class="sxs-lookup"><span data-stu-id="74421-151">**Create a master target server**: The master target server sends and receives failback data.</span></span> <span data-ttu-id="74421-152">您在內部部署環境中建立的管理伺服器預設會安裝主要目標伺服器。</span><span class="sxs-lookup"><span data-stu-id="74421-152">The management server you created on-premises has a master target server installed by default.</span></span> <span data-ttu-id="74421-153">不過，您可能必須根據容錯回復流量的大小，另外建立一個用來容錯回復的主要目標伺服器。</span><span class="sxs-lookup"><span data-stu-id="74421-153">However, depending on the volume of failed back traffic you might need to create a separate master target server for failback.</span></span>
  * <span data-ttu-id="74421-154">如果您想要另外建立一個在 Linux 上執行的主要目標伺服器，您必須先設定 Linux VM，然後才安裝主要目標伺服器，詳情如下所述。</span><span class="sxs-lookup"><span data-stu-id="74421-154">if you want to create an additional master target server running on Linux, you’ll need to set up the Linux VM before you install the master target server, as described below.</span></span>
* <span data-ttu-id="74421-155">執行容錯回復時，組態伺服器必須為內部部署。</span><span class="sxs-lookup"><span data-stu-id="74421-155">Configuration server is required on-premises when you do a failback.</span></span> <span data-ttu-id="74421-156">容錯回復期間，虛擬機器必須存在於組態伺服器資料庫中，而失敗的容錯回復將不會成功。</span><span class="sxs-lookup"><span data-stu-id="74421-156">During failback, the virtual machine must exist in the Configuration server database, failing which failback wont be successful.</span></span> <span data-ttu-id="74421-157">因此，請確定您定期排定備份伺服器。</span><span class="sxs-lookup"><span data-stu-id="74421-157">Hence ensure that you take regular scheduled backup of your server.</span></span> <span data-ttu-id="74421-158">發生災害時，您需要使用相同的 IP 位址進行還原，讓容錯回復運作。</span><span class="sxs-lookup"><span data-stu-id="74421-158">In case of a disaster, you will need to restore it with the same IP address so that failback will work.</span></span>

## <a name="set-up-the-process-server-in-azure"></a><span data-ttu-id="74421-159">在 Azure 中設定處理序伺服器</span><span class="sxs-lookup"><span data-stu-id="74421-159">Set up the process server in Azure</span></span>
<span data-ttu-id="74421-160">您必須在 Azure 中安裝處理序伺服器，讓 Azure VM 可以將資料傳回內部部署的主要目標伺服器。</span><span class="sxs-lookup"><span data-stu-id="74421-160">You need to install a process server in Azure so that the Azure VMs can send the data back to on-premises master target server.</span></span>

1. <span data-ttu-id="74421-161">在 [Site Recovery 入口網站] > [組態伺服器] 中，選擇新增處理序伺服器。</span><span class="sxs-lookup"><span data-stu-id="74421-161">In the Site Recovery portal >  **Configuration Servers** select to add a new process server.</span></span>

   ![](./media/site-recovery-failback-azure-to-vmware-classic/ps1.png)
2. <span data-ttu-id="74421-162">指定處理序伺服器名稱，然後輸入要用來以系統管理員身分連接到 Azure VM 的名稱和密碼。</span><span class="sxs-lookup"><span data-stu-id="74421-162">Specify a process server name, and enter a name and password you'll use to connect to the Azure VM as an administrator.</span></span> <span data-ttu-id="74421-163">在 [組態伺服器]  中，選取內部部署管理伺服器，並指定應該在其中部署處理序伺服器的 Azure 網路。</span><span class="sxs-lookup"><span data-stu-id="74421-163">In **Configuration Server** select the on-premises management server, and specify the Azure network in which the process server should be deployed.</span></span> <span data-ttu-id="74421-164">這應該是 Azure VM 所在的網路。</span><span class="sxs-lookup"><span data-stu-id="74421-164">This should be the network in which the Azure VMs are located.</span></span> <span data-ttu-id="74421-165">從選取子網路指定唯一的 IP 位址並開始部署。</span><span class="sxs-lookup"><span data-stu-id="74421-165">Specify a unique IP address from the select subnet and begin deployment.</span></span>

   ![](./media/site-recovery-failback-azure-to-vmware-classic/ps2.png)

   <span data-ttu-id="74421-166">將觸發部署處理序伺服器的作業</span><span class="sxs-lookup"><span data-stu-id="74421-166">A job to deploy the process server will be triggered</span></span>

   ![](./media/site-recovery-failback-azure-to-vmware-classic/ps3.png)

   <span data-ttu-id="74421-167">在 Azure 中部署處理序伺服器之後，您就能使用指定的認證來登入該伺服器。</span><span class="sxs-lookup"><span data-stu-id="74421-167">After the process server is deployed in Azure you can log onto it using the credentials you specified.</span></span> <span data-ttu-id="74421-168">第一次登入時，會執行處理序伺服器對話方塊。</span><span class="sxs-lookup"><span data-stu-id="74421-168">The first time you log in the process server dialog will run.</span></span> <span data-ttu-id="74421-169">請輸入內部部署管理伺服器的 IP 位址和它的複雜密碼。</span><span class="sxs-lookup"><span data-stu-id="74421-169">Type in the IP address of the on-premises management server and its passphrase.</span></span> <span data-ttu-id="74421-170">保留預設連接埠 443 設定。</span><span class="sxs-lookup"><span data-stu-id="74421-170">Leave the default port 443 setting.</span></span> <span data-ttu-id="74421-171">您也可以保留預設連接埠 9443 來進行資料複寫，除非您在設定內部部署管理伺服器時已特別修改此設定。</span><span class="sxs-lookup"><span data-stu-id="74421-171">You can also leave the default 9443 port for data replication unless you specifically modified this setting when you set up the on-premises management server.</span></span>

   > [!NOTE]
   > <span data-ttu-id="74421-172">該伺服器不會顯示在 [VM 屬性] 下。　</span><span class="sxs-lookup"><span data-stu-id="74421-172">The server won't be visible under **VM properties**.</span></span> <span data-ttu-id="74421-173">該伺服器只會顯示在它已註冊之管理伺服器的 [伺服器]  索引標籤底下。</span><span class="sxs-lookup"><span data-stu-id="74421-173">It's only visible under the **Servers** tab in the management server to which it's been registered.</span></span> <span data-ttu-id="74421-174">處理序伺服器大約需要 10-15 分鐘才會出現。</span><span class="sxs-lookup"><span data-stu-id="74421-174">It can take about 10-15 mins for the process server to appear.</span></span>
   >
   >

## <a name="set-up-the-master-target-server-on-premises"></a><span data-ttu-id="74421-175">在內部部署環境中設定主要目標伺服器</span><span class="sxs-lookup"><span data-stu-id="74421-175">Set up the master target server on-premises</span></span>
<span data-ttu-id="74421-176">主要目標伺服器會接收容錯回復資料。</span><span class="sxs-lookup"><span data-stu-id="74421-176">The master target server receives the failback data.</span></span> <span data-ttu-id="74421-177">主要目標伺服器會自動安裝在內部部署管理伺服器，但如果要容錯回復大量資料，則可能需要另外設定一個主要目標伺服器。</span><span class="sxs-lookup"><span data-stu-id="74421-177">A master target server is automatically installed on the on-premises management server but if you're failing back a lot of data you might need to set up an additional master target server.</span></span> <span data-ttu-id="74421-178">以下列方式來執行此動作：</span><span class="sxs-lookup"><span data-stu-id="74421-178">Do this as follows:</span></span>

> [!NOTE]
> <span data-ttu-id="74421-179">如果您想要在 Linux 上安裝主要目標伺服器，請遵循下一個程序中的指示。</span><span class="sxs-lookup"><span data-stu-id="74421-179">If you want to install a master target server on Linux, follow the instructions in the next procedure.</span></span>
>
>

1. <span data-ttu-id="74421-180">如果您要在 Windows 上安裝主要目標伺服器，請從要安裝主要目標伺服器的 VM 開啟 [快速啟動] 頁面，並下載 Azure Site Recovery 整合安裝精靈的安裝檔案。</span><span class="sxs-lookup"><span data-stu-id="74421-180">If you're installing the master target server on Windows, open the Quick Start page from the VM on which you're installing the master target server, and download the installation file for the Azure Site Recovery Unified Setup wizard.</span></span>
2. <span data-ttu-id="74421-181">執行安裝程式，並在 [開始之前] 中選取 [新增額外處理序伺服器以相應放大部署]。</span><span class="sxs-lookup"><span data-stu-id="74421-181">Run setup and in **Before you begin** select **Add additional process servers to scale out deployment**.</span></span>
3. <span data-ttu-id="74421-182">以您 [設定管理伺服器](site-recovery-vmware-to-azure-classic.md)時的相同方式完成精靈。</span><span class="sxs-lookup"><span data-stu-id="74421-182">Complete the wizard in the same way you did when you [set up the management server](site-recovery-vmware-to-azure-classic.md).</span></span> <span data-ttu-id="74421-183">在 [組態伺服器詳細資料]  頁面上，指定這個主要目標伺服器的 IP 位址和複雜密碼來存取 VM。</span><span class="sxs-lookup"><span data-stu-id="74421-183">On the **Configuration Server Details** page, specify the IP address of this master target server, and a passphrase to access the VM.</span></span>

### <a name="set-up-a-linux-vm-as-the-master-target-server"></a><span data-ttu-id="74421-184">將 Linux VM 設定為主要目標伺服器</span><span class="sxs-lookup"><span data-stu-id="74421-184">Set up a Linux VM as the master target server</span></span>
<span data-ttu-id="74421-185">若要將執行主要目標伺服器的管理伺服器設定為 Linux VM，您必須安裝 CentOS 6.6 最小作業系統、擷取每個 SCSI 硬碟的 SCSI 識別碼、額外安裝某些封裝，並套用一些自訂變更。</span><span class="sxs-lookup"><span data-stu-id="74421-185">To set up the management server running the master target server as a Linux VM you'll need to install the Cent)S 6.6 minimal operating system, retrieve the SCSI IDs for each SCSI hard disk, install some additional packages, and apply some custom changes.</span></span>

#### <a name="install-centos-66"></a><span data-ttu-id="74421-186">安裝 CentOS 6.6</span><span class="sxs-lookup"><span data-stu-id="74421-186">Install CentOS 6.6</span></span>
1. <span data-ttu-id="74421-187">在管理伺服器 VM 上安裝 CentOS 6.6 最小作業系統。</span><span class="sxs-lookup"><span data-stu-id="74421-187">Install the CentOS 6.6 minimal operating system on the management server VM.</span></span> <span data-ttu-id="74421-188">在 DVD 光碟機中放入 ISO，並啟動系統。</span><span class="sxs-lookup"><span data-stu-id="74421-188">Keep the ISO in a DVD drive and boot the system.</span></span> <span data-ttu-id="74421-189">略過媒體測試，於語言選取美式英文，選取 [基本存放裝置]確認硬碟機上沒有重要資料，然後按一下 [是] 以捨棄其中的所有資料。</span><span class="sxs-lookup"><span data-stu-id="74421-189">Skip the media testing, select US English at the language, select **Basic Storage Devices**, check that the hard drive doesn’t have any important data and click **Yes**, discard any data.</span></span> <span data-ttu-id="74421-190">輸入管理伺服器的主機名稱，然後選取伺服器的網路介面卡。</span><span class="sxs-lookup"><span data-stu-id="74421-190">Enter the host name of the management server and select the server network adapter.</span></span>  <span data-ttu-id="74421-191">在 [編輯系統] 對話方塊中，選取 [自動連線]，然後新增靜態 IP 位址、網路和 DNS 設定。</span><span class="sxs-lookup"><span data-stu-id="74421-191">In the **Editing System** dialog select** Connect automatically** and add a static IP address, network, and DNS settings.</span></span> <span data-ttu-id="74421-192">指定時區，以及用來存取管理伺服器的根密碼。</span><span class="sxs-lookup"><span data-stu-id="74421-192">Specify a time zone, and a root password to access the management server.</span></span>
2. <span data-ttu-id="74421-193">當系統要求您指定安裝類型時，請選取 [建立自訂配置] 做為磁碟分割。</span><span class="sxs-lookup"><span data-stu-id="74421-193">When you asked the type of installation you’d like select **Create Custom Layout** as the partition.</span></span> <span data-ttu-id="74421-194">按一下 [下一步] 之後，請選取 [免費] 並按一下 [建立]。</span><span class="sxs-lookup"><span data-stu-id="74421-194">After you click **Next** select **Free** and click Create.</span></span> <span data-ttu-id="74421-195">使用 [FS 類型：ext4] 來建立 **/**、**/var/crash** 和 **/home** 磁碟分割。</span><span class="sxs-lookup"><span data-stu-id="74421-195">Create **/**,  **/var/crash** and **/home partitions** with **FS Type:** **ext4**.</span></span> <span data-ttu-id="74421-196">以 [FS 類型：swap] 的形式建立交換磁碟分割。</span><span class="sxs-lookup"><span data-stu-id="74421-196">Create the swap partion as **FS Type: swap**.</span></span>
3. <span data-ttu-id="74421-197">如果找到現存裝置，將會出現警告訊息。</span><span class="sxs-lookup"><span data-stu-id="74421-197">If pre-existing devices are found a warning message will appear.</span></span> <span data-ttu-id="74421-198">按一下 [格式化]  以使用磁碟分割設定將該磁碟機格式化。</span><span class="sxs-lookup"><span data-stu-id="74421-198">Click **Format** to format the drive with the partition settings.</span></span> <span data-ttu-id="74421-199">按一下 [將變更寫入磁碟]  以套用磁碟分割變更。</span><span class="sxs-lookup"><span data-stu-id="74421-199">Click **Write change to disk** to apply the partition changes.</span></span>
4. <span data-ttu-id="74421-200">選取 [安裝開機載入器]  >  [下一步] 以在根磁碟分割上安裝開機載入器。</span><span class="sxs-lookup"><span data-stu-id="74421-200">Select **Install boot loader** > **Next** to install the boot loader on the root partition.</span></span>
5. <span data-ttu-id="74421-201">安裝完成時，按一下 [重新開機] 。</span><span class="sxs-lookup"><span data-stu-id="74421-201">When installation is complete click **Reboot**.</span></span>

#### <a name="retrieve-the-scsi-ids"></a><span data-ttu-id="74421-202">擷取 SCSI 識別碼</span><span class="sxs-lookup"><span data-stu-id="74421-202">Retrieve the SCSI IDs</span></span>
1. <span data-ttu-id="74421-203">在安裝後，擷取 VM 中每個 SCSI 硬碟的 SCSI 識別碼。</span><span class="sxs-lookup"><span data-stu-id="74421-203">After installation retrieve the SCSI IDs for each SCSI hard disk in the VM.</span></span> <span data-ttu-id="74421-204">若要執行此作業，請將管理伺服器 VM 關機，然後在 VMware 的 VM 屬性中以滑鼠右鍵按一下 VM 項目 > [編輯設定]  >  [選項]。</span><span class="sxs-lookup"><span data-stu-id="74421-204">To do this shut down the management server VM, in the VM properties in VMware right-click the VM entry > **Edit Settings** > **Options**.</span></span>
2. <span data-ttu-id="74421-205">選取 [進階]  >  [一般項目]，然後按一下 [組態參數]。</span><span class="sxs-lookup"><span data-stu-id="74421-205">Select **Advanced** > **General item** and click **Configuration Parameters**.</span></span> <span data-ttu-id="74421-206">機器在執行時會停用此選項。</span><span class="sxs-lookup"><span data-stu-id="74421-206">This option will be de-active when the machine is running.</span></span> <span data-ttu-id="74421-207">若要啟用，則必須將機器關閉。</span><span class="sxs-lookup"><span data-stu-id="74421-207">To make it active the machine must be shut down.</span></span>
3. <span data-ttu-id="74421-208">如果 **disk.EnableUUID** 資料列存在，請確定其值已設定為 [True] \(有區分大小寫)。</span><span class="sxs-lookup"><span data-stu-id="74421-208">If the row **disk.EnableUUID** exists make sure the value is set to **True** (case sensitive).</span></span> <span data-ttu-id="74421-209">如果已如此設定，則可加以取消，然後在機器開機之後，於客體作業系統內部測試 SCSI 命令。</span><span class="sxs-lookup"><span data-stu-id="74421-209">If it already is you can cancel and test the SCSI command inside a guest operating system after it’s booted.</span></span>
4. <span data-ttu-id="74421-210">如果該資料列不存在，請按一下 [新增資料列]，並以 [True] 值來新增它。</span><span class="sxs-lookup"><span data-stu-id="74421-210">If the row doesn’t existing click **Add Row** – and add it with the **True** value.</span></span> <span data-ttu-id="74421-211">請勿使用雙引號。</span><span class="sxs-lookup"><span data-stu-id="74421-211">Don’t use double-quotes.</span></span>

#### <a name="install-additional-packages"></a><span data-ttu-id="74421-212">安裝其他封裝</span><span class="sxs-lookup"><span data-stu-id="74421-212">Install additional packages</span></span>
<span data-ttu-id="74421-213">您必須另外下載並安裝一些封裝。</span><span class="sxs-lookup"><span data-stu-id="74421-213">You’ll need to download and install some additional packages.</span></span>

1. <span data-ttu-id="74421-214">確定主要目標伺服器已連線到網際網路。</span><span class="sxs-lookup"><span data-stu-id="74421-214">Make sure the master target server is connected to the internet.</span></span>
2. <span data-ttu-id="74421-215">執行下列命令以從 CentOS 儲存機制下載並安裝 15 個套件： **# yum install –y xfsprogs perl lsscsi rsync wget kexec-tools**。</span><span class="sxs-lookup"><span data-stu-id="74421-215">Run this command to download and install 15 packages from the CentOS repository: **# yum install –y xfsprogs perl lsscsi rsync wget kexec-tools**.</span></span>
3. <span data-ttu-id="74421-216">如果所要保護之來源機器的根目錄或開機裝置是執行 Linux wit Reiser 或 XFS 檔案系統，則應該下載並安裝下列額外封裝：</span><span class="sxs-lookup"><span data-stu-id="74421-216">If the source machines you’re protecting are running Linux wit Reiser or XFS file system for the root or boot device, then you should download and install additional packages as follows:</span></span>

   * # <a name="cd-usrlocal"></a><span data-ttu-id="74421-217">cd /usr/local</span><span class="sxs-lookup"><span data-stu-id="74421-217">cd /usr/local</span></span>
   * # <a name="wget-httpelrepoorglinuxelrepoel6x8664rpmskmod-reiserfs-00-1el6elrepox8664rpmhttpelrepoorglinuxelrepoel6x8664rpmskmod-reiserfs-00-1el6elrepox8664rpm"></a><span data-ttu-id="74421-218">wget [http://elrepo.org/linux/elrepo/el6/x86_64/RPMS/kmod-reiserfs-0.0-1.el6.elrepo.x86_64.rpm](http://elrepo.org/linux/elrepo/el6/x86_64/RPMS/kmod-reiserfs-0.0-1.el6.elrepo.x86_64.rpm)</span><span class="sxs-lookup"><span data-stu-id="74421-218">wget [http://elrepo.org/linux/elrepo/el6/x86_64/RPMS/kmod-reiserfs-0.0-1.el6.elrepo.x86_64.rpm](http://elrepo.org/linux/elrepo/el6/x86_64/RPMS/kmod-reiserfs-0.0-1.el6.elrepo.x86_64.rpm)</span></span>
   * # <a name="wget-httpelrepoorglinuxelrepoel6x8664rpmsreiserfs-utils-3621-1el6elrepox8664rpmhttpelrepoorglinuxelrepoel6x8664rpmsreiserfs-utils-3621-1el6elrepox8664rpm"></a><span data-ttu-id="74421-219">wget [http://elrepo.org/linux/elrepo/el6/x86_64/RPMS/reiserfs-utils-3.6.21-1.el6.elrepo.x86_64.rpm](http://elrepo.org/linux/elrepo/el6/x86_64/RPMS/reiserfs-utils-3.6.21-1.el6.elrepo.x86_64.rpm)</span><span class="sxs-lookup"><span data-stu-id="74421-219">wget [http://elrepo.org/linux/elrepo/el6/x86_64/RPMS/reiserfs-utils-3.6.21-1.el6.elrepo.x86_64.rpm](http://elrepo.org/linux/elrepo/el6/x86_64/RPMS/reiserfs-utils-3.6.21-1.el6.elrepo.x86_64.rpm)</span></span>
   * # <a name="rpm-ivh-kmod-reiserfs-00-1el6elrepox8664rpm-reiserfs-utils-3621-1el6elrepox8664rpm"></a><span data-ttu-id="74421-220">rpm –ivh kmod-reiserfs-0.0-1.el6.elrepo.x86_64.rpm reiserfs-utils-3.6.21-1.el6.elrepo.x86_64.rpm</span><span class="sxs-lookup"><span data-stu-id="74421-220">rpm –ivh kmod-reiserfs-0.0-1.el6.elrepo.x86_64.rpm reiserfs-utils-3.6.21-1.el6.elrepo.x86_64.rpm</span></span>
   * # <a name="wget-httpmirrorcentosorgcentos66osx8664packagesxfsprogs-311-16el6x8665rpmhttpmirrorcentosorgcentos66osx8664packagesxfsprogs-311-16el6x8665rpm"></a><span data-ttu-id="74421-221">wget [http://mirror.centos.org/centos/6.6/os/x86_64/Packages/xfsprogs-3.1.1-16.el6.x86_65.rpm](http://mirror.centos.org/centos/6.6/os/x86_64/Packages/xfsprogs-3.1.1-16.el6.x86_65.rpm)</span><span class="sxs-lookup"><span data-stu-id="74421-221">wget [http://mirror.centos.org/centos/6.6/os/x86_64/Packages/xfsprogs-3.1.1-16.el6.x86_65.rpm](http://mirror.centos.org/centos/6.6/os/x86_64/Packages/xfsprogs-3.1.1-16.el6.x86_65.rpm)</span></span>
   * # <a name="rpm-ivh-xfsprogs-311-16el6x8664rpm"></a><span data-ttu-id="74421-222">rpm –ivh xfsprogs-3.1.1-16.el6.x86_64.rpm</span><span class="sxs-lookup"><span data-stu-id="74421-222">rpm –ivh xfsprogs-3.1.1-16.el6.x86_64.rpm</span></span>

#### <a name="apply-custom-changes"></a><span data-ttu-id="74421-223">套用自訂變更</span><span class="sxs-lookup"><span data-stu-id="74421-223">Apply custom changes</span></span>
<span data-ttu-id="74421-224">完成後續安裝步驟並已安裝封裝之後，請執行下列動作以套用自訂變更：</span><span class="sxs-lookup"><span data-stu-id="74421-224">Do the following to apply custom changes after you’ve complete the post-installation steps and installed the packages:</span></span>

1. <span data-ttu-id="74421-225">將 RHEL 6-64 整合代理程式二進位檔複製到 VM。</span><span class="sxs-lookup"><span data-stu-id="74421-225">Copy the RHEL 6-64 Unified Agent binary to the VM.</span></span> <span data-ttu-id="74421-226">執行下列命令來解壓縮二進位檔：**tar –zxvf <file name>**</span><span class="sxs-lookup"><span data-stu-id="74421-226">Run this command to untar the binary: **tar –zxvf <file name>**</span></span>
2. <span data-ttu-id="74421-227">執行此命令來授與權限： **# chmod 755 ./ApplyCustomChanges.sh**</span><span class="sxs-lookup"><span data-stu-id="74421-227">Run this command to give permissions: **# chmod 755 ./ApplyCustomChanges.sh**</span></span>
3. <span data-ttu-id="74421-228">執行指令碼： **# ./ApplyCustomChanges.sh**。</span><span class="sxs-lookup"><span data-stu-id="74421-228">Run the script: **# ./ApplyCustomChanges.sh**.</span></span> <span data-ttu-id="74421-229">指令碼只應執行一次。</span><span class="sxs-lookup"><span data-stu-id="74421-229">You should only run the script once.</span></span> <span data-ttu-id="74421-230">在指令碼順利執行後，請重新啟動伺服器。</span><span class="sxs-lookup"><span data-stu-id="74421-230">Reboot the server after the script runs successfully.</span></span>

## <a name="run-the-failback"></a><span data-ttu-id="74421-231">執行容錯回復</span><span class="sxs-lookup"><span data-stu-id="74421-231">Run the failback</span></span>
### <a name="reprotect-the-azure-vms"></a><span data-ttu-id="74421-232">重新保護 Azure VM</span><span class="sxs-lookup"><span data-stu-id="74421-232">Reprotect the Azure VMs</span></span>
1. <span data-ttu-id="74421-233">在 Site Recovery 入口網站 > [機器] 索引標籤中，選取已容錯移轉的 VM，然後按一下 [重新保護]。</span><span class="sxs-lookup"><span data-stu-id="74421-233">In the Site Recovery  portal > **Machines** tab select the VM that's been failed over and click **Re-Protect**.</span></span>
2. <span data-ttu-id="74421-234">在 [主要目標伺服器] 和 [處理序伺服器] 中，選取內部部署主要目標伺服器和 Azure VM 處理序伺服器。</span><span class="sxs-lookup"><span data-stu-id="74421-234">In **Master Target Server** and **Process Server** select the on-premises master target server, and the Azure VM process server.</span></span>
3. <span data-ttu-id="74421-235">選取設定用來連線到 VM 的帳戶。</span><span class="sxs-lookup"><span data-stu-id="74421-235">Select the account you configured for connecting to the VM.</span></span>
4. <span data-ttu-id="74421-236">選取保護群組的容錯回復版本。</span><span class="sxs-lookup"><span data-stu-id="74421-236">Select the failback version of the protection group.</span></span> <span data-ttu-id="74421-237">例如，如果是在 PG1 中保護 VM，則必須選取 PG1_Failback。</span><span class="sxs-lookup"><span data-stu-id="74421-237">For example if the VM is protected in PG1 then you'll need to select PG1_Failback.</span></span>
5. <span data-ttu-id="74421-238">如果您想要復原到替代位置，請選取為主要目標伺服器所設定的保留磁碟機和資料存放區。</span><span class="sxs-lookup"><span data-stu-id="74421-238">If you want to recover to an alternate location, select the retention drive and datastore configured for the master target server.</span></span> <span data-ttu-id="74421-239">當您容錯回復到內部部署網站時，容錯回復保護計畫中的 VMware VM 會使用與主要目標伺服器相同的資料存放區。</span><span class="sxs-lookup"><span data-stu-id="74421-239">When you fail back to the on-premises site the VMware VMs in the failback protection plan will use the same datastore as the master target server.</span></span> <span data-ttu-id="74421-240">如果您想要將複本 Azure VM 復原到相同的內部部署 VM，則內部部署 VM 必須已位於與主要目標伺服器相同的資料存放區內。</span><span class="sxs-lookup"><span data-stu-id="74421-240">If you want to recover the replica Azure VM to the same on-premises VM then the on-premises VM should already be in the same datastore as the master target server.</span></span> <span data-ttu-id="74421-241">如果內部部署環境中沒有任何 VM，則會在重新保護期間建立一個新的 VM。</span><span class="sxs-lookup"><span data-stu-id="74421-241">If there's no VM on-premises a new one will be created during reprotection.</span></span>

   ![](./media/site-recovery-failback-azure-to-vmware-classic/failback1.png)
6. <span data-ttu-id="74421-242">在您按一下 [確定]  以開始重新保護之後，就會開始執行將 VM 從 Azure 複寫到內部部署網站的作業。</span><span class="sxs-lookup"><span data-stu-id="74421-242">After you click **OK** to begin reprotection a job begins to replicate the VM from Azure to the on-premises site.</span></span> <span data-ttu-id="74421-243">您可以在 [作業]  索引標籤上追蹤進度。</span><span class="sxs-lookup"><span data-stu-id="74421-243">You can track the progress on the **Jobs** tab.</span></span>

   ![](./media/site-recovery-failback-azure-to-vmware-classic/failback2.png)

### <a name="run-a-failover-to-the-on-premises-site"></a><span data-ttu-id="74421-244">執行容錯移轉至內部部署網站</span><span class="sxs-lookup"><span data-stu-id="74421-244">Run a failover to the on-premises site</span></span>
<span data-ttu-id="74421-245">在重新保護之後，VM 會轉換為其保護群組的容錯回復版本，並且會自動新增到用於容錯移轉至 Azure 的復原計畫中 (如果已有計畫)。</span><span class="sxs-lookup"><span data-stu-id="74421-245">After reprotection the VM is moved to the failback version of its protection group and is automatically added to the recovery plan you used for the failover to Azure if there is one.</span></span>

1. <span data-ttu-id="74421-246">在 [復原方案] 頁面中，選取包含容錯回復群組的復原方案，然後按一下 [容錯移轉]  >  [非計劃性容錯移轉]。</span><span class="sxs-lookup"><span data-stu-id="74421-246">In the **Recovery Plans** page select the recovery plan containing the failback group and click **Failover** > **Unplanned Failover**.</span></span>
2. <span data-ttu-id="74421-247">在 [確認容錯移轉]  中，確認容錯移轉方向 (至 Azure)，然後選取您要用於容錯移轉的復原點 (最新)。</span><span class="sxs-lookup"><span data-stu-id="74421-247">In **Confirm Failover** verify the failover direction (to Azure) and select the recovery point you want to use for the failover (latest).</span></span> <span data-ttu-id="74421-248">如果您已在設定複寫屬性時啟用 [多部 VM]  ，您便可以復原到最新的應用程式或與當機時保持一致的復原點。</span><span class="sxs-lookup"><span data-stu-id="74421-248">If you enabled **Multi-VM** when you configured replication properties you can recover to the latest app or crash-consistent recovery point.</span></span> <span data-ttu-id="74421-249">按一下核取記號以啟動容錯移轉。</span><span class="sxs-lookup"><span data-stu-id="74421-249">Click the check mark to start the failover.</span></span>
3. <span data-ttu-id="74421-250">在容錯移轉期間，Site Recovery 會關閉 Azure VM。</span><span class="sxs-lookup"><span data-stu-id="74421-250">During failover Site Recovery will shut down the Azure VMs.</span></span> <span data-ttu-id="74421-251">在確認容錯回復已如預期完成後，您可以確認 Azure VM 是否已如預期般關閉。</span><span class="sxs-lookup"><span data-stu-id="74421-251">After you check that failback has completed as expected you can you can check that the Azure VMs have been shut down as expected.</span></span>

### <a name="reprotect-the--on-premises-site"></a><span data-ttu-id="74421-252">重新保護內部部署網站</span><span class="sxs-lookup"><span data-stu-id="74421-252">Reprotect the  on-premises site</span></span>
<span data-ttu-id="74421-253">在容錯回復完成後，內部部署網站上的資料會恢復，但不會受到保護。</span><span class="sxs-lookup"><span data-stu-id="74421-253">After failback completes your data will be back on the on-premises site, but won’t be protected.</span></span> <span data-ttu-id="74421-254">若要再次啟動複寫至 Azure，請執行下列動作：</span><span class="sxs-lookup"><span data-stu-id="74421-254">To start replicating to Azure again do the following:</span></span>

1. <span data-ttu-id="74421-255">在 Site Recovery 入口網站 > [機器] 索引標籤中，選取已容錯回復的 VM，然後按一下 [重新保護]。</span><span class="sxs-lookup"><span data-stu-id="74421-255">In the Site Recovery portal > **Machines** tab select the VMs that have failed back and click **Re-Protect**.</span></span>
2. <span data-ttu-id="74421-256">確認複寫至 Azure 已正常運作之後，您可以在 Azure 中刪除已容錯回復的 Azure VM (目前已未執行)。</span><span class="sxs-lookup"><span data-stu-id="74421-256">After you verify that replication to Azure is working as expected, in Azure you can delete the Azure VMs (currently not running) that were failed back.</span></span>

### <a name="common-issues-in-failback"></a><span data-ttu-id="74421-257">容錯回復的常見問題</span><span class="sxs-lookup"><span data-stu-id="74421-257">Common Issues in failback</span></span>
1. <span data-ttu-id="74421-258">如果您執行唯讀的使用者 vCenter 探索並保護虛擬機器，這樣會成功且容錯回復可作用。</span><span class="sxs-lookup"><span data-stu-id="74421-258">If you perform Read-Only User vCenter discovery and protect virtual machines, it succeeds and failover works.</span></span> <span data-ttu-id="74421-259">在重新保護的時候則會失敗，因為探索不到資料存放區。</span><span class="sxs-lookup"><span data-stu-id="74421-259">At the time of Reprotect, it will fail since the datastores cannot be discovered.</span></span> <span data-ttu-id="74421-260">徵兆是重新保護的時候不會列出資料存放區。</span><span class="sxs-lookup"><span data-stu-id="74421-260">As a symptom you will not see the datastores listed while re-protecting.</span></span> <span data-ttu-id="74421-261">若要解決此問題，您可以使用有適當權限的帳戶更新 vCenter 認證並重試作業。</span><span class="sxs-lookup"><span data-stu-id="74421-261">To resolve this, you can update the vCenter credential with appropriate account that has permissions and retry the job.</span></span> [<span data-ttu-id="74421-262">閱讀更多資訊</span><span class="sxs-lookup"><span data-stu-id="74421-262">Read more</span></span>](site-recovery-vmware-to-azure-classic.md)
2. <span data-ttu-id="74421-263">當您將 Linux VM 容錯回復並在內部執行它時，您會看到 Network Manager 封裝已從該機器解除安裝。</span><span class="sxs-lookup"><span data-stu-id="74421-263">When you failback a Linux VM and run it on-prem, you will see that the Network Manager package is uninstalled from the machine.</span></span> <span data-ttu-id="74421-264">這是因為 VM 在 Azure 中復原時，Network Manager 封裝遭移除。</span><span class="sxs-lookup"><span data-stu-id="74421-264">This is because when the VM is recovered in Azure, the Network Manager package is removed.</span></span>
3. <span data-ttu-id="74421-265">當 VM 是以靜態 IP 位址設定且容錯移轉至 Azure 時，IP 位址則是透過 DHCP 取得。</span><span class="sxs-lookup"><span data-stu-id="74421-265">When a VM is configured with Static IP address and is failed over to Azure, the IP address is acquired via DHCP.</span></span> <span data-ttu-id="74421-266">當您容錯移轉至內部時，該 VM 會繼續使用 DHCP 取得 IP 位址。</span><span class="sxs-lookup"><span data-stu-id="74421-266">When you fail over back to On-prem, the VM continues to use DHCP to acquire the IP address.</span></span> <span data-ttu-id="74421-267">如有需要，則您必須手動登入該機器並將 IP 位址設為原本的靜態位址。</span><span class="sxs-lookup"><span data-stu-id="74421-267">You will need to manually login into the machine and set the IP address back to Static address if required.</span></span>
4. <span data-ttu-id="74421-268">如果您是使用 ESXi 5.5 免費版或 vSphere 6 Hypervisor 免費版，則容錯移轉會成功，但容錯回復不會成功。</span><span class="sxs-lookup"><span data-stu-id="74421-268">If you are using either ESXi 5.5 free edition or vSphere 6 Hypervisor free edition, failover would succeed, but failback will not succeed.</span></span> <span data-ttu-id="74421-269">您將需要升級為評估授權才能啟用容錯回復。</span><span class="sxs-lookup"><span data-stu-id="74421-269">You will ned to upgrade to either Evaluation License to enable failback.</span></span>

## <a name="failing-back-with-expressroute"></a><span data-ttu-id="74421-270">透過 ExpressRoute 容錯回復</span><span class="sxs-lookup"><span data-stu-id="74421-270">Failing back with ExpressRoute</span></span>
<span data-ttu-id="74421-271">您可以透過 VPN 連線或 Azure ExpressRoute 進行容錯回復。</span><span class="sxs-lookup"><span data-stu-id="74421-271">You can fail back over a VPN connection or Azure ExpressRoute.</span></span> <span data-ttu-id="74421-272">如果您想要使用 ExpressRoute，請注意下列事項：</span><span class="sxs-lookup"><span data-stu-id="74421-272">If you want to use ExpressRoute note the following:</span></span>

* <span data-ttu-id="74421-273">ExpressRoute 應該設定在來源機器所要容錯移轉到的 Azure 虛擬網路上，以及容錯移轉發生時 Azure VM 所位於的 Azure 虛擬網路上。</span><span class="sxs-lookup"><span data-stu-id="74421-273">ExpressRoute should be set up on the Azure virtual network to which source machines fail over, and in which Azure VMs are located after the failover occurs.</span></span>
* <span data-ttu-id="74421-274">資料會複寫至公用端點上的 Azure 儲存體帳戶。</span><span class="sxs-lookup"><span data-stu-id="74421-274">Data is replicated to an Azure storage account on a public endpoint.</span></span> <span data-ttu-id="74421-275">您應該在 ExpressRoute 中設定與目標資料中心的公用對等互連，這樣一來，Site Recovery 複寫才能使用 ExpressRoute。</span><span class="sxs-lookup"><span data-stu-id="74421-275">You should set up public peering in ExpressRoute with the target data center for Site Recovery replication to use ExpressRoute.</span></span>
