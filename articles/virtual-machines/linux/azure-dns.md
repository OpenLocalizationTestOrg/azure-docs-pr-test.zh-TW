---
title: "Azure 中 Linux 虛擬機器的 DNS 名稱解析選項"
description: "Azure IaaS 中 Linux 虛擬機器的名稱解析案例 (包括提供的 DNS 服務)：混合式外部 DNS 和自備 DNS 伺服器。"
services: virtual-machines
documentationcenter: na
author: RicksterCDN
manager: timlt
editor: tysonn
ms.assetid: 787a1e04-cebf-4122-a1b4-1fcf0a2bbf5f
ms.service: virtual-machines-linux
ms.devlang: na
ms.topic: article
ms.tgt_pltfrm: na
ms.workload: infrastructure-services
ms.date: 10/19/2016
ms.author: rclaus
ms.openlocfilehash: cc06ee9305b4d3034154a0825c1aea53fe446f80
ms.sourcegitcommit: f537befafb079256fba0529ee554c034d73f36b0
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 07/11/2017
---
# <a name="dns-name-resolution-options-for-linux-virtual-machines-in-azure"></a><span data-ttu-id="0ce13-103">Azure 中 Linux 虛擬機器的 DNS 名稱解析選項</span><span class="sxs-lookup"><span data-stu-id="0ce13-103">DNS Name Resolution options for Linux virtual machines in Azure</span></span>
<span data-ttu-id="0ce13-104">Azure 預設會提供單一虛擬網路中所有虛擬機器的 DNS 名稱解析。</span><span class="sxs-lookup"><span data-stu-id="0ce13-104">Azure provides DNS name resolution by default for all virtual machines that are in a single virtual network.</span></span> <span data-ttu-id="0ce13-105">您可以在 Azure 託管的虛擬機器上設定專屬 DNS 服務，以實作專屬 DNS 名稱解析解決方案。</span><span class="sxs-lookup"><span data-stu-id="0ce13-105">You can implement your own DNS name resolution solution by configuring your own DNS services on your virtual machines that Azure hosts.</span></span> <span data-ttu-id="0ce13-106">下列案例應該可協助您選擇哪一種適合您的情況。</span><span class="sxs-lookup"><span data-stu-id="0ce13-106">The following scenarios should help you choose the one that works for your situation.</span></span>

* [<span data-ttu-id="0ce13-107">Azure 提供的名稱解析</span><span class="sxs-lookup"><span data-stu-id="0ce13-107">Name resolution that Azure provides</span></span>](#azure-provided-name-resolution)
* [<span data-ttu-id="0ce13-108">使用專屬 DNS 伺服器的名稱解析</span><span class="sxs-lookup"><span data-stu-id="0ce13-108">Name resolution using your own DNS server</span></span>](#name-resolution-using-your-own-dns-server)

<span data-ttu-id="0ce13-109">您使用的名稱解析類型取決於虛擬機器和角色執行個體如何彼此通訊。</span><span class="sxs-lookup"><span data-stu-id="0ce13-109">The type of name resolution that you use depends on how your virtual machines and role instances need to communicate with each other.</span></span>

<span data-ttu-id="0ce13-110">下表說明各種案例和對應的名稱解析解決方案：</span><span class="sxs-lookup"><span data-stu-id="0ce13-110">The following table illustrates scenarios and corresponding name resolution solutions:</span></span>

| <span data-ttu-id="0ce13-111">**案例**</span><span class="sxs-lookup"><span data-stu-id="0ce13-111">**Scenario**</span></span> | <span data-ttu-id="0ce13-112">**方案**</span><span class="sxs-lookup"><span data-stu-id="0ce13-112">**Solution**</span></span> | <span data-ttu-id="0ce13-113">**尾碼**</span><span class="sxs-lookup"><span data-stu-id="0ce13-113">**Suffix**</span></span> |
| --- | --- | --- |
| <span data-ttu-id="0ce13-114">相同虛擬網路中的角色執行個體或虛擬機器之間的名稱解析</span><span class="sxs-lookup"><span data-stu-id="0ce13-114">Name resolution between role instances or virtual machines in the same virtual network</span></span> |[<span data-ttu-id="0ce13-115">Azure 提供的名稱解析</span><span class="sxs-lookup"><span data-stu-id="0ce13-115">Name resolution that Azure provides</span></span>](#azure-provided-name-resolution) |<span data-ttu-id="0ce13-116">主機名稱或完整網域名稱 (FQDN)</span><span class="sxs-lookup"><span data-stu-id="0ce13-116">hostname or fully-qualified domain name (FQDN)</span></span> |
| <span data-ttu-id="0ce13-117">不同虛擬網路中的角色執行個體或虛擬機器之間的名稱解析</span><span class="sxs-lookup"><span data-stu-id="0ce13-117">Name resolution between role instances or virtual machines in different virtual networks</span></span> |<span data-ttu-id="0ce13-118">客戶管理的 DNS 伺服器將虛擬網路之間的查詢轉送供 Azure (DNS Proxy) 解析。</span><span class="sxs-lookup"><span data-stu-id="0ce13-118">Customer-managed DNS servers that forward queries between virtual networks for resolution by Azure (DNS proxy).</span></span> <span data-ttu-id="0ce13-119">請參閱[使用專屬 DNS 伺服器的名稱解析](#name-resolution-using-your-own-dns-server)。</span><span class="sxs-lookup"><span data-stu-id="0ce13-119">See [Name resolution using your own DNS server](#name-resolution-using-your-own-dns-server).</span></span> |<span data-ttu-id="0ce13-120">僅 FQDN</span><span class="sxs-lookup"><span data-stu-id="0ce13-120">FQDN only</span></span> |
| <span data-ttu-id="0ce13-121">解析 Azure 中角色執行個體或虛擬機器的內部部署電腦及伺服器名稱</span><span class="sxs-lookup"><span data-stu-id="0ce13-121">Resolution of on-premises computers and service names from role instances or virtual machines in Azure</span></span> |<span data-ttu-id="0ce13-122">客戶管理的 DNS 伺服器 (例如，內部部署的網域控制站、本機唯讀網域控制站或使用區域傳輸同步的次要 DNS)。</span><span class="sxs-lookup"><span data-stu-id="0ce13-122">Customer-managed DNS servers (for example, on-premises domain controller, local read-only domain controller, or a DNS secondary synced by using zone transfers).</span></span> <span data-ttu-id="0ce13-123">請參閱[使用專屬 DNS 伺服器的名稱解析](#name-resolution-using-your-own-dns-server)。</span><span class="sxs-lookup"><span data-stu-id="0ce13-123">See [Name resolution using your own DNS server](#name-resolution-using-your-own-dns-server).</span></span> |<span data-ttu-id="0ce13-124">僅 FQDN</span><span class="sxs-lookup"><span data-stu-id="0ce13-124">FQDN only</span></span> |
| <span data-ttu-id="0ce13-125">從內部部署電腦解析 Azure 主機名稱</span><span class="sxs-lookup"><span data-stu-id="0ce13-125">Resolution of Azure hostnames from on-premises computers</span></span> |<span data-ttu-id="0ce13-126">將查詢轉送到所對應虛擬網路中客戶管理的 DNS Proxy 伺服器。</span><span class="sxs-lookup"><span data-stu-id="0ce13-126">Forward queries to a customer-managed DNS proxy server in the corresponding virtual network.</span></span> <span data-ttu-id="0ce13-127">Proxy 伺服器將查詢轉送給 Azure 進行解析。</span><span class="sxs-lookup"><span data-stu-id="0ce13-127">The proxy server forwards queries to Azure for resolution.</span></span> <span data-ttu-id="0ce13-128">請參閱[使用專屬 DNS 伺服器的名稱解析](#name-resolution-using-your-own-dns-server)。</span><span class="sxs-lookup"><span data-stu-id="0ce13-128">See [Name resolution using your own DNS server](#name-resolution-using-your-own-dns-server).</span></span> |<span data-ttu-id="0ce13-129">僅 FQDN</span><span class="sxs-lookup"><span data-stu-id="0ce13-129">FQDN only</span></span> |
| <span data-ttu-id="0ce13-130">內部 IP 的反向 DNS</span><span class="sxs-lookup"><span data-stu-id="0ce13-130">Reverse DNS for internal IPs</span></span> |[<span data-ttu-id="0ce13-131">使用專屬 DNS 伺服器的名稱解析</span><span class="sxs-lookup"><span data-stu-id="0ce13-131">Name resolution using your own DNS server</span></span>](#name-resolution-using-your-own-dns-server) |<span data-ttu-id="0ce13-132">n/a</span><span class="sxs-lookup"><span data-stu-id="0ce13-132">n/a</span></span> |

## <a name="name-resolution-that-azure-provides"></a><span data-ttu-id="0ce13-133">Azure 提供的名稱解析</span><span class="sxs-lookup"><span data-stu-id="0ce13-133">Name resolution that Azure provides</span></span>
<span data-ttu-id="0ce13-134">除了公用 DNS 名稱的解析之外，Azure 也提供相同虛擬網路中的虛擬機器和角色執行個體的內部名稱解析。</span><span class="sxs-lookup"><span data-stu-id="0ce13-134">Along with resolution of public DNS names, Azure provides internal name resolution for virtual machines and role instances that are in the same virtual network.</span></span> <span data-ttu-id="0ce13-135">在 Azure Resource Manager 型虛擬網路中，DNS 尾碼在虛擬網路之間是一致的，因此不需要 FQDN。</span><span class="sxs-lookup"><span data-stu-id="0ce13-135">In virtual networks that are based on Azure Resource Manager, the DNS suffix is consistent across the virtual network; the FQDN is not needed.</span></span> <span data-ttu-id="0ce13-136">DNS 名稱可以指派給網路介面卡 (NIC) 和虛擬機器。</span><span class="sxs-lookup"><span data-stu-id="0ce13-136">DNS names can be assigned to both network interface cards (NICs) and virtual machines.</span></span> <span data-ttu-id="0ce13-137">雖然 Azure 提供的名稱解析不需要任何設定，但它不適用於所有部署案例，如上表所示。</span><span class="sxs-lookup"><span data-stu-id="0ce13-137">Although the name resolution that Azure provides does not require any configuration, it is not the appropriate choice for all deployment scenarios, as seen on the preceding table.</span></span>

### <a name="features-and-considerations"></a><span data-ttu-id="0ce13-138">功能和注意事項</span><span class="sxs-lookup"><span data-stu-id="0ce13-138">Features and considerations</span></span>
<span data-ttu-id="0ce13-139">**功能：**</span><span class="sxs-lookup"><span data-stu-id="0ce13-139">**Features:**</span></span>

* <span data-ttu-id="0ce13-140">不需要設定，就能使用 Azure 提供的名稱解析。</span><span class="sxs-lookup"><span data-stu-id="0ce13-140">No configuration is required to use name resolution that Azure provides.</span></span>
* <span data-ttu-id="0ce13-141">Azure 提供的名稱解析服務高度可用。</span><span class="sxs-lookup"><span data-stu-id="0ce13-141">The name resolution service that Azure provides is highly available.</span></span> <span data-ttu-id="0ce13-142">您不需要建立和管理專屬 DNS 伺服器的叢集。</span><span class="sxs-lookup"><span data-stu-id="0ce13-142">You don't need to create and manage clusters of your own DNS servers.</span></span>
* <span data-ttu-id="0ce13-143">Azure 提供的名稱解析服務可與專屬 DNS 伺服器搭配使用，以解析內部部署及 Azure 主機名稱。</span><span class="sxs-lookup"><span data-stu-id="0ce13-143">The name resolution service that Azure provides can be used along with your own DNS servers to resolve both on-premises and Azure hostnames.</span></span>
* <span data-ttu-id="0ce13-144">在虛擬網路的虛擬機器之間提供名稱解析，不需要 FQDN。</span><span class="sxs-lookup"><span data-stu-id="0ce13-144">Name resolution is provided between virtual machines in virtual networks without need for the FQDN.</span></span>
* <span data-ttu-id="0ce13-145">您可以使用最能描述部署的主機名稱，而不是使用自動產生的名稱。</span><span class="sxs-lookup"><span data-stu-id="0ce13-145">You can use hostnames that best describe your deployments rather than working with auto-generated names.</span></span>

<bpt id="p1">**</bpt>Considerations:<ept id="p1">**</ept>

* <span data-ttu-id="0ce13-147">Azure 建立的 DNS 尾碼不能修改。</span><span class="sxs-lookup"><span data-stu-id="0ce13-147">The DNS suffix that Azure creates cannot be modified.</span></span>
* <span data-ttu-id="0ce13-148">您無法手動註冊您自己的記錄。</span><span class="sxs-lookup"><span data-stu-id="0ce13-148">You cannot manually register your own records.</span></span>
* <span data-ttu-id="0ce13-149">不支援 WINS 和 NetBIOS。</span><span class="sxs-lookup"><span data-stu-id="0ce13-149">WINS and NetBIOS are not supported.</span></span>
* <span data-ttu-id="0ce13-150">主機名稱必須是 DNS 相容。</span><span class="sxs-lookup"><span data-stu-id="0ce13-150">Hostnames must be DNS-compatible.</span></span>
    <span data-ttu-id="0ce13-151">名稱只能使用 0-9、a-z 和 '-'，無法以 '-' 開始或結束。</span><span class="sxs-lookup"><span data-stu-id="0ce13-151">Names must use only 0-9, a-z, and '-', and they cannot start or end with a '-'.</span></span> <span data-ttu-id="0ce13-152">請參閱 RFC 3696 第 2 節。</span><span class="sxs-lookup"><span data-stu-id="0ce13-152">See RFC 3696 Section 2.</span></span>
* <span data-ttu-id="0ce13-153">每部虛擬機器的 DNS 查詢流量已經過節流。</span><span class="sxs-lookup"><span data-stu-id="0ce13-153">DNS query traffic is throttled for each virtual machine.</span></span> <span data-ttu-id="0ce13-154">節流應該不會影響大部分的應用程式。</span><span class="sxs-lookup"><span data-stu-id="0ce13-154">Throttling shouldn't impact most applications.</span></span>  <span data-ttu-id="0ce13-155">如果觀察到要求節流，請確定用戶端快取已啟用。</span><span class="sxs-lookup"><span data-stu-id="0ce13-155">If request throttling is observed, ensure that client-side caching is enabled.</span></span>  <span data-ttu-id="0ce13-156">如需詳細資訊，請參閱[充分利用 Azure 提供的名稱解析](#getting-the-most-from-name-resolution-that-azure-provides)。</span><span class="sxs-lookup"><span data-stu-id="0ce13-156">For more information, see [Getting the most from name resolution that Azure provides](#getting-the-most-from-name-resolution-that-azure-provides).</span></span>

### <a name="getting-the-most-from-name-resolution-that-azure-provides"></a><span data-ttu-id="0ce13-157">充分利用 Azure 提供的名稱解析</span><span class="sxs-lookup"><span data-stu-id="0ce13-157">Getting the most from name resolution that Azure provides</span></span>
<span data-ttu-id="0ce13-158">**用戶端快取：**</span><span class="sxs-lookup"><span data-stu-id="0ce13-158">**Client-side caching:**</span></span>

<span data-ttu-id="0ce13-159">某些 DNS 查詢不會跨網路傳送。</span><span class="sxs-lookup"><span data-stu-id="0ce13-159">Some DNS queries are not sent across the network.</span></span> <span data-ttu-id="0ce13-160">用戶端快取可藉由解析本機快取的週期性 DNS 查詢，協助減少延遲以及改善網路不一致的恢復能力。</span><span class="sxs-lookup"><span data-stu-id="0ce13-160">Client-side caching helps reduce latency and improve resilience to network inconsistencies by resolving recurring DNS queries from a local cache.</span></span> <span data-ttu-id="0ce13-161">DNS 記錄包含存留時間 (TTL)，可讓快取盡可能長時間儲存記錄而不會影響記錄的有效性。</span><span class="sxs-lookup"><span data-stu-id="0ce13-161">DNS records contain a Time-To-Live (TTL), which enables the cache to store the record for as long as possible without impacting record freshness.</span></span> <span data-ttu-id="0ce13-162">因此，用戶端快取適用於大部分的情況。</span><span class="sxs-lookup"><span data-stu-id="0ce13-162">As a result, client-side caching is suitable for most situations.</span></span>

<span data-ttu-id="0ce13-163">某些 Linux 發行版本預設不包含快取功能。</span><span class="sxs-lookup"><span data-stu-id="0ce13-163">Some Linux distributions do not include caching by default.</span></span> <span data-ttu-id="0ce13-164">建議您在已經確認沒有本機快取之後，為每部 Linux 虛擬機器新增快取。</span><span class="sxs-lookup"><span data-stu-id="0ce13-164">We recommend that you add a cache to each Linux virtual machine after you check that there isn't a local cache already.</span></span>

<span data-ttu-id="0ce13-165">有數個不同的 DNS 快取封裝可供使用，例如 dnsmasq。</span><span class="sxs-lookup"><span data-stu-id="0ce13-165">Several different DNS caching packages, such as dnsmasq, are available.</span></span> <span data-ttu-id="0ce13-166">以下是在最常見的發行版本上安裝 dnsmasq 的步驟：</span><span class="sxs-lookup"><span data-stu-id="0ce13-166">Here are the steps to install dnsmasq on the most common distributions:</span></span>

<span data-ttu-id="0ce13-167">**Ubuntu (使用 resolvconf)**</span><span class="sxs-lookup"><span data-stu-id="0ce13-167">**Ubuntu (uses resolvconf)**</span></span>
  * <span data-ttu-id="0ce13-168">安裝 dnsmasq 封裝 (“sudo apt-get install dnsmasq”)。</span><span class="sxs-lookup"><span data-stu-id="0ce13-168">Install the dnsmasq package (“sudo apt-get install dnsmasq”).</span></span>

<span data-ttu-id="0ce13-169">SUSE (使用 netconf)：</span><span class="sxs-lookup"><span data-stu-id="0ce13-169">**SUSE (uses netconf)**:</span></span>
1. <span data-ttu-id="0ce13-170">安裝 dnsmasq 封裝 (“sudo zypper install dnsmasq”)。</span><span class="sxs-lookup"><span data-stu-id="0ce13-170">Install the dnsmasq package (“sudo zypper install dnsmasq”).</span></span>
2. <span data-ttu-id="0ce13-171">啟用 dnsmasq 服務 (“systemctl enable dnsmasq.service”)。</span><span class="sxs-lookup"><span data-stu-id="0ce13-171">Enable the dnsmasq service (“systemctl enable dnsmasq.service”).</span></span>
3. <span data-ttu-id="0ce13-172">啟動 dnsmasq 服務 (“systemctl start dnsmasq.service”)。</span><span class="sxs-lookup"><span data-stu-id="0ce13-172">Start the dnsmasq service (“systemctl start dnsmasq.service”).</span></span>
4. <span data-ttu-id="0ce13-173">編輯 “/etc/sysconfig/network/config” 並將 NETCONFIG_DNS_FORWARDER="" 變更為 ”dnsmasq”。</span><span class="sxs-lookup"><span data-stu-id="0ce13-173">Edit “/etc/sysconfig/network/config”, and change NETCONFIG_DNS_FORWARDER="" to ”dnsmasq”.</span></span>
5. <span data-ttu-id="0ce13-174">更新 resolv.conf ("netconfig update") 來設定快取作為本機 DNS 解析程式。</span><span class="sxs-lookup"><span data-stu-id="0ce13-174">Update resolv.conf ("netconfig update") to set the cache as the local DNS resolver.</span></span>

<span data-ttu-id="0ce13-175">**CentOS by Rogue Wave Software (先前稱為 OpenLogic；使用 NetworkManager)**</span><span class="sxs-lookup"><span data-stu-id="0ce13-175">**CentOS by Rogue Wave Software (formerly OpenLogic; uses NetworkManager)**</span></span>
1. <span data-ttu-id="0ce13-176">安裝 dnsmasq 封裝 (“sudo yum install dnsmasq”)。</span><span class="sxs-lookup"><span data-stu-id="0ce13-176">Install the dnsmasq package (“sudo yum install dnsmasq”).</span></span>
2. <span data-ttu-id="0ce13-177">啟用 dnsmasq 服務 (“systemctl enable dnsmasq.service”)。</span><span class="sxs-lookup"><span data-stu-id="0ce13-177">Enable the dnsmasq service (“systemctl enable dnsmasq.service”).</span></span>
3. <span data-ttu-id="0ce13-178">啟動 dnsmasq 服務 (“systemctl start dnsmasq.service”)。</span><span class="sxs-lookup"><span data-stu-id="0ce13-178">Start the dnsmasq service (“systemctl start dnsmasq.service”).</span></span>
4. <span data-ttu-id="0ce13-179">將 “prepend domain-name-servers 127.0.0.1;” 新增至 “/etc/dhclient-eth0.conf”。</span><span class="sxs-lookup"><span data-stu-id="0ce13-179">Add “prepend domain-name-servers 127.0.0.1;” to “/etc/dhclient-eth0.conf”.</span></span>
5. <span data-ttu-id="0ce13-180">重新啟動網路服務 (「服務網路重新啟動」) 來設定快取作為本機 DNS 解析程式</span><span class="sxs-lookup"><span data-stu-id="0ce13-180">Restart the network service (“service network restart”) to set the cache as the local DNS resolver</span></span>

> [!NOTE]
> <span data-ttu-id="0ce13-181">：'dnsmasq' 封裝只是許多適用於 Linux 之 DNS 快取的其中一個。</span><span class="sxs-lookup"><span data-stu-id="0ce13-181">: The 'dnsmasq' package is only one of the many DNS caches that are available for Linux.</span></span> <span data-ttu-id="0ce13-182">在您使用它之前，請檢查您需求的適用性，而且沒有安裝其他快取。</span><span class="sxs-lookup"><span data-stu-id="0ce13-182">Before you use it, check its suitability for your needs and that no other cache is installed.</span></span>
>
>

<span data-ttu-id="0ce13-183">**用戶端重試**</span><span class="sxs-lookup"><span data-stu-id="0ce13-183">**Client-side retries**</span></span>

<span data-ttu-id="0ce13-184">DNS 主要是 UDP 通訊協定。</span><span class="sxs-lookup"><span data-stu-id="0ce13-184">DNS is primarily a UDP protocol.</span></span> <span data-ttu-id="0ce13-185">因為 UDP 通訊協定並不保證訊息傳遞，所以 DNS 通訊協定本身會處理重試邏輯。</span><span class="sxs-lookup"><span data-stu-id="0ce13-185">Because the UDP protocol doesn't guarantee message delivery, the DNS protocol itself handles retry logic.</span></span> <span data-ttu-id="0ce13-186">每個 DNS 用戶端 (作業系統) 可以展現不同的重試邏輯，根據建立者喜好設定而定：</span><span class="sxs-lookup"><span data-stu-id="0ce13-186">Each DNS client (operating system) can exhibit different retry logic depending on the creator's preference:</span></span>

* <span data-ttu-id="0ce13-187">Windows 作業系統會在 1 秒後重試，然後再依序隔 2、4、4 秒後重試。</span><span class="sxs-lookup"><span data-stu-id="0ce13-187">Windows operating systems retry after one second and then again after another two, four, and another four seconds.</span></span>
* <span data-ttu-id="0ce13-188">預設 Linux 安裝程式會在 5 秒之後重試。</span><span class="sxs-lookup"><span data-stu-id="0ce13-188">The default Linux setup retries after five seconds.</span></span>  <span data-ttu-id="0ce13-189">您應該將此變更為以 1 秒的間隔重試 5 次。</span><span class="sxs-lookup"><span data-stu-id="0ce13-189">You should change this to retry five times at one-second intervals.</span></span>  

<span data-ttu-id="0ce13-190">檢查 Linux 虛擬機器上目前的設定 'cat /etc/resolv.conf'，並查看 'options' 行，例如：</span><span class="sxs-lookup"><span data-stu-id="0ce13-190">To check the current settings on a Linux virtual machine, 'cat /etc/resolv.conf', and look at the 'options' line, for example:</span></span>

    options timeout:1 attempts:5

<span data-ttu-id="0ce13-191">resolv.conf 檔案會自動產生且不可編輯。</span><span class="sxs-lookup"><span data-stu-id="0ce13-191">The resolv.conf file is auto-generated and should not be edited.</span></span> <span data-ttu-id="0ce13-192">新增 [選項] 行的特定步驟會因發行版本而有所不同：</span><span class="sxs-lookup"><span data-stu-id="0ce13-192">The specific steps that add the 'options' line vary by distribution:</span></span>

<span data-ttu-id="0ce13-193">**Ubuntu** (使用 resolvconf)</span><span class="sxs-lookup"><span data-stu-id="0ce13-193">**Ubuntu** (uses resolvconf)</span></span>
1. <span data-ttu-id="0ce13-194">將選項行新增至 '/etc/resolveconf/resolv.conf.d/head'。</span><span class="sxs-lookup"><span data-stu-id="0ce13-194">Add the options line to '/etc/resolveconf/resolv.conf.d/head'.</span></span>
2. <span data-ttu-id="0ce13-195">執行 'resolvconf -u' 以進行更新。</span><span class="sxs-lookup"><span data-stu-id="0ce13-195">Run 'resolvconf -u' to update.</span></span>

<span data-ttu-id="0ce13-196">**SUSE** (使用 netconf)</span><span class="sxs-lookup"><span data-stu-id="0ce13-196">**SUSE** (uses netconf)</span></span>
1. <span data-ttu-id="0ce13-197">將 'timeout:1 attempts:5' 新增至 '/etc/sysconfig/network/config' 中的 NETCONFIG_DNS_RESOLVER_OPTIONS="" 參數。</span><span class="sxs-lookup"><span data-stu-id="0ce13-197">Add 'timeout:1 attempts:5' to the NETCONFIG_DNS_RESOLVER_OPTIONS="" parameter in '/etc/sysconfig/network/config'.</span></span>
2. <span data-ttu-id="0ce13-198">執行 'netconfig update' 以進行更新。</span><span class="sxs-lookup"><span data-stu-id="0ce13-198">Run 'netconfig update' to update.</span></span>

<span data-ttu-id="0ce13-199">**CentOS by Rogue Wave Software (先前稱為 OpenLogic)** (使用 NetworkManager)</span><span class="sxs-lookup"><span data-stu-id="0ce13-199">**CentOS by Rogue Wave Software (formerly OpenLogic)** (uses NetworkManager)</span></span>
1. <span data-ttu-id="0ce13-200">將 'echo "options timeout:1 attempts:5"' 新增至 '/etc/NetworkManager/dispatcher.d/11-dhclient'。</span><span class="sxs-lookup"><span data-stu-id="0ce13-200">Add 'echo "options timeout:1 attempts:5"' to '/etc/NetworkManager/dispatcher.d/11-dhclient'.</span></span>
2. <span data-ttu-id="0ce13-201">執行 'service network restart' 以進行更新。</span><span class="sxs-lookup"><span data-stu-id="0ce13-201">Run 'service network restart' to update.</span></span>

## <a name="name-resolution-using-your-own-dns-server"></a><span data-ttu-id="0ce13-202">使用專屬 DNS 伺服器的名稱解析</span><span class="sxs-lookup"><span data-stu-id="0ce13-202">Name resolution using your own DNS server</span></span>
<span data-ttu-id="0ce13-203">您的名稱解析需求可能超過 Azure 所提供的功能。</span><span class="sxs-lookup"><span data-stu-id="0ce13-203">Your name resolution needs may go beyond the features that Azure provides.</span></span> <span data-ttu-id="0ce13-204">例如，您可能需要虛擬網路之間的 DNS 解析。</span><span class="sxs-lookup"><span data-stu-id="0ce13-204">For example, you might require DNS resolution between virtual networks.</span></span> <span data-ttu-id="0ce13-205">為了涵蓋此案例，您可以使用專屬 DNS 伺服器。</span><span class="sxs-lookup"><span data-stu-id="0ce13-205">To cover this scenario, you can use your own DNS servers.</span></span>  

<span data-ttu-id="0ce13-206">虛擬網路內的 DNS 伺服器可以將 DNS 查詢轉送到 Azure 的遞迴解析程式，以解析相同虛擬網路中的主機名稱。</span><span class="sxs-lookup"><span data-stu-id="0ce13-206">DNS servers within a virtual network can forward DNS queries to recursive resolvers of Azure to resolve hostnames that are in the same virtual network.</span></span> <span data-ttu-id="0ce13-207">例如，在 Azure 中執行的 DNS 伺服器可以回應其專屬 DNS 區域檔案的 DNS 查詢，並將所有其他查詢轉送到 Azure。</span><span class="sxs-lookup"><span data-stu-id="0ce13-207">For example, a DNS server that runs in Azure can respond to DNS queries for its own DNS zone files and forward all other queries to Azure.</span></span> <span data-ttu-id="0ce13-208">這項功能可讓虛擬機器查看您區域檔案中的項目，以及 Azure 提供的主機名稱 (透過轉寄站)。</span><span class="sxs-lookup"><span data-stu-id="0ce13-208">This functionality enables virtual machines to see both your entries in your zone files and hostnames that Azure provides (via the forwarder).</span></span> <span data-ttu-id="0ce13-209">Azure 遞迴解析程式的存取是透過虛擬 IP 168.63.129.16 所提供。</span><span class="sxs-lookup"><span data-stu-id="0ce13-209">Access to the recursive resolvers of Azure is provided via the virtual IP 168.63.129.16.</span></span>

<span data-ttu-id="0ce13-210">DNS 轉送也允許虛擬網路之間的 DNS 解析，並可讓您的內部部署電腦解析 Azure 提供的主機名稱。</span><span class="sxs-lookup"><span data-stu-id="0ce13-210">DNS forwarding also enables DNS resolution between virtual networks and enables your on-premises machines to resolve hostnames that Azure provides.</span></span> <span data-ttu-id="0ce13-211">為了解析虛擬機器的主機名稱，DNS 伺服器虛擬機器必須位於同一個虛擬網路中，且設定為將主機名稱查詢轉送到 Azure。</span><span class="sxs-lookup"><span data-stu-id="0ce13-211">To resolve a virtual machine's hostname, the DNS server virtual machine must reside in the same virtual network and be configured to forward hostname queries to Azure.</span></span> <span data-ttu-id="0ce13-212">因為每個虛擬網路的 DNS 尾碼都不同，所以您可以使用條件性轉送規則來將 DNS 查詢傳送到正確的虛擬網路進行解析。</span><span class="sxs-lookup"><span data-stu-id="0ce13-212">Because the DNS suffix is different in each virtual network, you can use conditional forwarding rules to send DNS queries to the correct virtual network for resolution.</span></span> <span data-ttu-id="0ce13-213">下圖顯示使用此方法進行虛擬網路間 DNS 解析的兩個虛擬網路及一個內部部署網路：</span><span class="sxs-lookup"><span data-stu-id="0ce13-213">The following image shows two virtual networks and an on-premises network doing DNS resolution between virtual networks by using this method:</span></span>

![虛擬網路之間的 DNS 解析](./media/azure-dns/inter-vnet-dns.png)

<span data-ttu-id="0ce13-215">當您使用 Azure 提供的名稱解析時，會提供內部 DNS 尾碼給每部使用 DHCP 的虛擬機器。</span><span class="sxs-lookup"><span data-stu-id="0ce13-215">When you use name resolution that Azure provides, the internal DNS suffix is provided to each virtual machine by using DHCP.</span></span> <span data-ttu-id="0ce13-216">當您使用自己的名稱解析解決方案時，不會提供此尾碼給虛擬機器，因為此尾碼會干擾其他 DNS 架構。</span><span class="sxs-lookup"><span data-stu-id="0ce13-216">When you use your own name resolution solution, this suffix is not supplied to virtual machines because the suffix interferes with other DNS architectures.</span></span> <span data-ttu-id="0ce13-217">若要透過 FQDN 參照電腦，或要設定虛擬機器上的尾碼，您可以使用 PowerShell 或 API 來決定尾碼：</span><span class="sxs-lookup"><span data-stu-id="0ce13-217">To refer to machines by FQDN or to configure the suffix on your virtual machines, you can use PowerShell or the API to determine the suffix:</span></span>

* <span data-ttu-id="0ce13-218">針對 Azure Resource Manager 所管理的虛擬網路，可透過[網路介面卡](https://msdn.microsoft.com/library/azure/mt163668.aspx)資源取得尾碼。</span><span class="sxs-lookup"><span data-stu-id="0ce13-218">For virtual networks that are managed by Azure Resource Manager, the suffix is available via the [network interface card](https://msdn.microsoft.com/library/azure/mt163668.aspx) resource.</span></span> <span data-ttu-id="0ce13-219">您也可以執行 `azure network public-ip show <resource group> <pip name>` 命令來顯示您公用 IP 的詳細資料 (包括 NIC 的 FQDN)。</span><span class="sxs-lookup"><span data-stu-id="0ce13-219">You can also run the `azure network public-ip show <resource group> <pip name>` command to display the details of your public IP, which includes the FQDN of the NIC.</span></span>

<span data-ttu-id="0ce13-220">如果將查詢轉送到 Azure 不符合您的需求，您就需要提供專屬的 DNS 解決方案。</span><span class="sxs-lookup"><span data-stu-id="0ce13-220">If forwarding queries to Azure doesn't suit your needs, you need to provide your own DNS solution.</span></span>  <span data-ttu-id="0ce13-221">您的 DNS 解決方案需要：</span><span class="sxs-lookup"><span data-stu-id="0ce13-221">Your DNS solution needs to:</span></span>

* <span data-ttu-id="0ce13-222">提供適當的主機名稱解析，例如透過 [DDNS](../../virtual-network/virtual-networks-name-resolution-ddns.md)。</span><span class="sxs-lookup"><span data-stu-id="0ce13-222">Provide appropriate hostname resolution, for example via [DDNS](../../virtual-network/virtual-networks-name-resolution-ddns.md).</span></span> <span data-ttu-id="0ce13-223">如果您使用 DDNS，則可能需要停用 DNS 記錄清除。</span><span class="sxs-lookup"><span data-stu-id="0ce13-223">If you use DDNS, you might need to disable DNS record scavenging.</span></span> <span data-ttu-id="0ce13-224">Azure 的 DHCP 租用期很長，而清除可能會提前移除 DNS 記錄。</span><span class="sxs-lookup"><span data-stu-id="0ce13-224">DHCP leases of Azure are very long and scavenging may remove DNS records prematurely.</span></span>
* <span data-ttu-id="0ce13-225">提供適當的遞迴解析來允許外部網域名稱的解析。</span><span class="sxs-lookup"><span data-stu-id="0ce13-225">Provide appropriate recursive resolution to allow resolution of external domain names.</span></span>
* <span data-ttu-id="0ce13-226">可從其服務的用戶端存取 (連接埠 53 上的 TCP 和 UDP)，且能夠存取網際網路。</span><span class="sxs-lookup"><span data-stu-id="0ce13-226">Be accessible (TCP and UDP on port 53) from the clients it serves and be able to access the Internet.</span></span>
* <span data-ttu-id="0ce13-227">受保護以防止來自網際網路的存取，降低外部代理程式的威脅。</span><span class="sxs-lookup"><span data-stu-id="0ce13-227">Be secured against access from the Internet to mitigate threats posed by external agents.</span></span>

> [!NOTE]
> <span data-ttu-id="0ce13-228">為了達到最佳效能，當您在 Azure DNS 伺服器中使用虛擬機器時，請停用 IPv6，並將[執行個體層級公用 IP](../../virtual-network/virtual-networks-instance-level-public-ip.md) 指派給每部 DNS 伺服器虛擬機器。</span><span class="sxs-lookup"><span data-stu-id="0ce13-228">For best performance, when you use virtual machines in Azure DNS servers, disable IPv6 and assign an [Instance-Level Public IP](../../virtual-network/virtual-networks-instance-level-public-ip.md) to each DNS server virtual machine.</span></span>  
>
>
