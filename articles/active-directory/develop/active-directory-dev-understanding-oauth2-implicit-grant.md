---
title: "aaaUnderstanding hello OAuth2 隱含授與流程在 Azure AD 中 |Microsoft 文件"
description: "深入了解 Azure Active Directory 的實作 hello OAuth2 隱含授與流程，以及它是否適合您的應用程式。"
services: active-directory
documentationcenter: dev-center-name
author: jmprieur
manager: mbaldwin
editor: 
ms.assetid: 90e42ff9-43b0-4b4f-a222-51df847b2a8d
ms.service: active-directory
ms.devlang: na
ms.topic: article
ms.tgt_pltfrm: na
ms.workload: identity
ms.date: 11/15/2016
ms.author: jmprieur
ms.custom: aaddev
ms.openlocfilehash: 3402e6619709e1a5b1e790ffd79dc62139552d9d
ms.sourcegitcommit: 523283cc1b3c37c428e77850964dc1c33742c5f0
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 10/06/2017
---
# <a name="understanding-hello-oauth2-implicit-grant-flow-in-azure-active-directory-ad"></a><span data-ttu-id="1f45b-103">了解 hello OAuth2 隱含授與流程在 Azure Active Directory (AD)</span><span class="sxs-lookup"><span data-stu-id="1f45b-103">Understanding hello OAuth2 implicit grant flow in Azure Active Directory (AD)</span></span>
<span data-ttu-id="1f45b-104">hello OAuth2 隱含授予是昭彰的 hello 授與具有 hello hello OAuth2 規格中的安全性顧慮的最長的清單。</span><span class="sxs-lookup"><span data-stu-id="1f45b-104">hello OAuth2 implicit grant is notorious for being hello grant with hello longest list of security concerns in hello OAuth2 specification.</span></span> <span data-ttu-id="1f45b-105">而棒的是，這是 hello ADAL JS 和一個撰寫 SPA 應用程式時，我們建議您使用 hello 所實作的方法。</span><span class="sxs-lookup"><span data-stu-id="1f45b-105">And yet, that is hello approach implemented by ADAL JS and hello one we recommend when writing SPA applications.</span></span> <span data-ttu-id="1f45b-106">為何會這樣呢？</span><span class="sxs-lookup"><span data-stu-id="1f45b-106">What gives?</span></span> <span data-ttu-id="1f45b-107">它是所有的權衡取捨:，且證明，hello 隱含授予 hello 最好的方法，您可以嘗試使用 Web API 透過 JavaScript 從瀏覽器的應用程式。</span><span class="sxs-lookup"><span data-stu-id="1f45b-107">It’s all a matter of tradeoffs: and as it turns out, hello implicit grant is hello best approach you can pursue for applications that consume a Web API via JavaScript from a browser.</span></span>

## <a name="what-is-hello-oauth2-implicit-grant"></a><span data-ttu-id="1f45b-108">Hello OAuth2 隱含授予是什麼？</span><span class="sxs-lookup"><span data-stu-id="1f45b-108">What is hello OAuth2 implicit grant?</span></span>
<span data-ttu-id="1f45b-109">典型的 hello [OAuth2 授權碼授與](https://tools.ietf.org/html/rfc6749#section-1.3.1)是 hello 授權授與它使用兩個單獨的端點。</span><span class="sxs-lookup"><span data-stu-id="1f45b-109">hello quintessential [OAuth2 authorization code grant](https://tools.ietf.org/html/rfc6749#section-1.3.1) is hello authorization grant which uses two separate endpoints.</span></span> <span data-ttu-id="1f45b-110">hello 使用者互動階段中，會導致授權程式碼會使用 hello 授權端點。</span><span class="sxs-lookup"><span data-stu-id="1f45b-110">hello authorization endpoint is used for hello user interaction phase, which results in an authorization code.</span></span> <span data-ttu-id="1f45b-111">hello 用戶端接著會使用 hello 語彙基元端點來交換存取權杖，且經常重新整理權杖的 hello 程式碼。</span><span class="sxs-lookup"><span data-stu-id="1f45b-111">hello token endpoint is then used by hello client for exchanging hello code for an access token, and often a refresh token as well.</span></span> <span data-ttu-id="1f45b-112">Web 應用程式是必要的 toopresent 他們自己的應用程式認證 toohello 權杖端點，如此 hello 授權伺服器可以驗證 hello 用戶端。</span><span class="sxs-lookup"><span data-stu-id="1f45b-112">Web applications are required toopresent their own application credentials toohello token endpoint, so that hello authorization server can authenticate hello client.</span></span>

<span data-ttu-id="1f45b-113">hello [OAuth2 隱含授予](https://tools.ietf.org/html/rfc6749#section-1.3.2)是其他授權授與。</span><span class="sxs-lookup"><span data-stu-id="1f45b-113">hello [OAuth2 implicit grant](https://tools.ietf.org/html/rfc6749#section-1.3.2) is a variant of other authorization grants.</span></span> <span data-ttu-id="1f45b-114">它可讓用戶端 tooobtain 存取權杖 (id_token 時使用和[OpenId Connect](http://openid.net/specs/openid-connect-core-1_0.html)) 直接從 hello 授權端點，而不連絡 hello 權杖端點，也不驗證 hello 用戶端。</span><span class="sxs-lookup"><span data-stu-id="1f45b-114">It allows a client tooobtain an access token (and id_token, when using [OpenId Connect](http://openid.net/specs/openid-connect-core-1_0.html)) directly from hello authorization endpoint, without contacting hello token endpoint nor authenticating hello client.</span></span> <span data-ttu-id="1f45b-115">此變異，專為以 JavaScript 為基礎的 Web 瀏覽器中執行的應用程式： hello 原始 OAuth2 規格，在語彙基元會傳回 URI 片段中。</span><span class="sxs-lookup"><span data-stu-id="1f45b-115">This variant was specifically designed for JavaScript based applications running in a Web browser: in hello original OAuth2 specification, tokens are returned in a URI fragment.</span></span> <span data-ttu-id="1f45b-116">會讓 hello 語彙基元的位元可用 toohello JavaScript 程式碼在 hello 用戶端，但它可以保證不會納入朝 hello 伺服器的重新導向。</span><span class="sxs-lookup"><span data-stu-id="1f45b-116">That makes hello token bits available toohello JavaScript code in hello client, but it guarantees they won’t be included in redirects toward hello server.</span></span> <span data-ttu-id="1f45b-117">直接從 hello 授權端點傳回語彙基元，透過瀏覽器重新導向。</span><span class="sxs-lookup"><span data-stu-id="1f45b-117">Returning tokens via browser redirects directly from hello authorization endpoint.</span></span> <span data-ttu-id="1f45b-118">它也有 hello 優點消除跨原始呼叫，也就是 hello JavaScript 應用程式是否需要的 toocontact hello 權杖端點所需的任何需求。</span><span class="sxs-lookup"><span data-stu-id="1f45b-118">It also has hello advantage of eliminating any requirements for cross origin calls, which are necessary if hello JavaScript application is required toocontact hello token endpoint.</span></span>

<span data-ttu-id="1f45b-119">Hello OAuth2 隱含授予的重要特性在於這類流程永遠不會傳回重新整理語彙基元 toohello 用戶端的 hello 事實。</span><span class="sxs-lookup"><span data-stu-id="1f45b-119">An important characteristic of hello OAuth2 implicit grant is hello fact that such flows never return refresh tokens toohello client.</span></span> <span data-ttu-id="1f45b-120">我們將會看到 hello 下一節中，，不是真有必要，因此實際上會有安全性問題。</span><span class="sxs-lookup"><span data-stu-id="1f45b-120">As we will see in hello next section, that isn’t really necessary and would in fact be a security issue.</span></span>

## <a name="suitable-scenarios-for-hello-oauth2-implicit-grant"></a><span data-ttu-id="1f45b-121">授與的 hello OAuth2 隱含適合案例</span><span class="sxs-lookup"><span data-stu-id="1f45b-121">Suitable scenarios for hello OAuth2 implicit grant</span></span>
<span data-ttu-id="1f45b-122">Hello OAuth2 規格本身宣告，如 hello 隱含授予已設計的 tooenable 使用者代理程式的應用程式 – 是 toosay，瀏覽器中執行的 JavaScript 應用程式。</span><span class="sxs-lookup"><span data-stu-id="1f45b-122">As hello OAuth2 specification itself declares, hello implicit grant has been devised tooenable user-agent applications – that is toosay, JavaScript applications executing within a browser.</span></span> <span data-ttu-id="1f45b-123">hello 定義這類應用程式的特性是 JavaScript 程式碼，用來存取伺服器資源 (通常是 Web API) 及 hello 應用程式 UX 據此更新。</span><span class="sxs-lookup"><span data-stu-id="1f45b-123">hello defining characteristic of such applications is that JavaScript code is used for accessing server resources (typically a Web API) and for updating hello application UX accordingly.</span></span> <span data-ttu-id="1f45b-124">考慮應用程式，例如 Gmail 或 Outlook Web Access： 當您選取訊息收件匣中，只有 hello 訊息，視覺效果面板變更 toodisplay hello 新選取項目，同時 hello hello 頁面其餘部分維持不修改。</span><span class="sxs-lookup"><span data-stu-id="1f45b-124">Think of applications like Gmail or Outlook Web Access: when you select a message from your inbox, only hello message visualization panel changes toodisplay hello new selection, while hello rest of hello page remains unmodified.</span></span> <span data-ttu-id="1f45b-125">這是相較於傳統重新導向式 Web 應用程式，其中每個使用者互動會導致完整網頁回傳和新的伺服器回應 hello 整頁轉譯。</span><span class="sxs-lookup"><span data-stu-id="1f45b-125">This is in contrast with traditional redirect-based Web apps, where every user interaction results in a full page postback and a full page rendering of hello new server response.</span></span>

<span data-ttu-id="1f45b-126">應用程式來充分 hello JavaScript 為基礎的方法 tooits 極端稱為單一頁面應用程式或 SPAs: hello 概念是，這些應用程式只有提供初始的 HTML 網頁和相關聯的 JavaScript 含有的所有後續的互動透過 JavaScript 執行 web 應用程式開發介面呼叫。</span><span class="sxs-lookup"><span data-stu-id="1f45b-126">Applications that take hello JavaScript based approach tooits extreme are called Single Page Applications, or SPAs: hello idea is that those applications only serve an initial HTML page and associated JavaScript, with all subsequent interactions being driven by Web API calls performed via JavaScript.</span></span> <span data-ttu-id="1f45b-127">不過，混合式方法，其中 hello 應用程式大致回傳導向，但執行偶爾 JS 呼叫，並不常見，-也是相關的 hello 隱含流程使用方式的討論。</span><span class="sxs-lookup"><span data-stu-id="1f45b-127">However, hybrid approaches, where hello application is mostly postback-driven but performs occasional JS calls, are not uncommon – hello discussion about implicit flow usage is relevant for those as well.</span></span>

<span data-ttu-id="1f45b-128">重新導向型應用程式通常會透過 Cookie 來保護其要求，不過，這種方法並非也適用於 JavaScript 應用程式。</span><span class="sxs-lookup"><span data-stu-id="1f45b-128">Redirect-based applications typically secure their requests via cookies, however, that approach does not work as well for JavaScript applications.</span></span> <span data-ttu-id="1f45b-129">Cookie 用於 hello 它們已產生的當 JavaScript 呼叫可能會導向其他網域的網域。</span><span class="sxs-lookup"><span data-stu-id="1f45b-129">Cookies only work against hello domain they have been generated for, while JavaScript calls might be directed toward other domains.</span></span> <span data-ttu-id="1f45b-130">事實上，經常會 hello 案例： 考慮叫用 Microsoft Graph API，Office 應用程式開發介面，Azure API – 從 hello 應用程式提供其中 hello 網域之外的所有位於應用程式。</span><span class="sxs-lookup"><span data-stu-id="1f45b-130">In fact, that will frequently be hello case: think of applications invoking Microsoft Graph API, Office API, Azure API – all residing outside hello domain from where hello application is served.</span></span> <span data-ttu-id="1f45b-131">JavaScript 應用程式的趨勢日益是 toohave 任何後端，其商務功能信賴憑證者在第 3 個合作對象的 Web Api tooimplement 100%。</span><span class="sxs-lookup"><span data-stu-id="1f45b-131">A growing trend for JavaScript applications is toohave no backend at all, relying 100% on 3rd party Web APIs tooimplement their business function.</span></span>

<span data-ttu-id="1f45b-132">目前，hello 保護呼叫 tooa Web 應用程式開發介面的慣用的方法是 toouse hello OAuth2 bearer 權杖的方法，其中每個呼叫會伴隨 OAuth2 存取權杖。</span><span class="sxs-lookup"><span data-stu-id="1f45b-132">Currently, hello preferred method of protecting calls tooa Web API is toouse hello OAuth2 bearer token approach, where every call is accompanied by an OAuth2 access token.</span></span> <span data-ttu-id="1f45b-133">hello Web API 會檢查 hello 連入的存取權杖，而如果它在中找到它 hello 必要的範圍，它會授與存取 toohello 要求的作業。</span><span class="sxs-lookup"><span data-stu-id="1f45b-133">hello Web API examines hello incoming access token and, if it finds in it hello necessary scopes, it grants access toohello requested operation.</span></span> <span data-ttu-id="1f45b-134">hello 隱含流程提供方便的機制，JavaScript 應用程式 tooobtain 存取權杖的 Web API，供應項目中的有很多優點尊重 toocookies:</span><span class="sxs-lookup"><span data-stu-id="1f45b-134">hello implicit flow provides a convenient mechanism for JavaScript applications tooobtain access tokens for a Web API, offering numerous advantages in respect toocookies:</span></span>

* <span data-ttu-id="1f45b-135">語彙基元可以可靠地取得而不需要跨原始呼叫 – 強制註冊 hello 重新導向 URI toowhich 語彙基元會傳回可保證，語彙基元是不脫離頁面</span><span class="sxs-lookup"><span data-stu-id="1f45b-135">Tokens can be reliably obtained without any need for cross origin calls – mandatory registration of hello redirect URI toowhich tokens are return guarantees that tokens are not displaced</span></span>
* <span data-ttu-id="1f45b-136">JavaScript 應用程式可以針對任意數量的鎖定 Web API 取得所需數量的存取權杖 – 不限網域</span><span class="sxs-lookup"><span data-stu-id="1f45b-136">JavaScript applications can obtain as many access tokens as they need, for as many Web APIs they target – with no restriction on domains</span></span>
* <span data-ttu-id="1f45b-137">HTML5 功能類似工作階段或本機儲存體而 cookie 管理是不透明 toohello 應用程式授與完整控制權杖快取和存留期管理</span><span class="sxs-lookup"><span data-stu-id="1f45b-137">HTML5 features like session or local storage grant full control over token caching and lifetime management, whereas cookies management is opaque toohello app</span></span>
* <span data-ttu-id="1f45b-138">存取權杖不容易受到 tooCross 站台要求偽造 (CSRF) 攻擊</span><span class="sxs-lookup"><span data-stu-id="1f45b-138">Access tokens aren’t susceptible tooCross-site request forgery (CSRF) attacks</span></span>

<span data-ttu-id="1f45b-139">hello 隱含授與流程不會發出重新整理權杖，主要是基於安全性考量。</span><span class="sxs-lookup"><span data-stu-id="1f45b-139">hello implicit grant flow does not issue refresh tokens, mostly for security reasons.</span></span> <span data-ttu-id="1f45b-140">重新整理權杖的範圍不像存取權杖那麼窄，前者會授與更多權力，因此萬一洩露出去，將會造成更大的損害。在 hello 隱含流程中，hello URL 中傳遞權杖，因此攔截 hello 風險高於 hello 授權碼授與。</span><span class="sxs-lookup"><span data-stu-id="1f45b-140">A refresh token isn’t as narrowly scoped as access tokens, granting far more power hence inflicting far more damage in case it is leaked out. In hello implicit flow, tokens are delivered in hello URL, hence hello risk of interception is higher than in hello authorization code grant.</span></span>

<span data-ttu-id="1f45b-141">不過請注意 JavaScript 應用程式有另一個機制，在進行處置時的更新存取權杖，而不會重複提示 hello 使用者提供認證。</span><span class="sxs-lookup"><span data-stu-id="1f45b-141">However, note that a JavaScript application has another mechanism at its disposal for renewing access tokens without repeatedly prompting hello user for credentials.</span></span> <span data-ttu-id="1f45b-142">hello 應用程式可以使用隱藏的 iframe tooperform 新權杖要求對 hello Azure AD 授權端點： 只要 hello 瀏覽器仍有作用中的工作階段 (讀取： 有一個工作階段 cookie) 針對 hello Azure AD 網域 hello 驗證要求順利執行而不需要使用者互動。</span><span class="sxs-lookup"><span data-stu-id="1f45b-142">hello application can use a hidden iframe tooperform new token requests against hello authorization endpoint of Azure AD: as long as hello browser still has an active session (read: has a session cookie) against hello Azure AD domain, hello authentication request can successfully occur without any need for user interaction.</span></span>

<span data-ttu-id="1f45b-143">此模型會授與 hello JavaScript 應用程式 hello 能力 tooindependently 更新存取權杖，而且即使取得新 api 的新的 （前提是 hello 使用者之前同意它們。</span><span class="sxs-lookup"><span data-stu-id="1f45b-143">This model grants hello JavaScript application hello ability tooindependently renew access tokens and even acquire new ones for a new API (provided that hello user previously consented for them.</span></span> <span data-ttu-id="1f45b-144">這可避免 hello 加入的負擔取得、 維護和保護高價值成品，例如重新整理權杖。</span><span class="sxs-lookup"><span data-stu-id="1f45b-144">This avoids hello added burden of acquiring, maintaining, and protecting a high value artifact such as a refresh token.</span></span> <span data-ttu-id="1f45b-145">hello 成品這樣 hello 無訊息更新能，hello Azure AD 工作階段 cookie，管理 hello 應用程式之外。</span><span class="sxs-lookup"><span data-stu-id="1f45b-145">hello artifact which makes hello silent renewal possible, hello Azure AD session cookie, is managed outside of hello application.</span></span> <span data-ttu-id="1f45b-146">這種方法的另一個好處是使用者可以登出任何登入 Azure AD 中，執行任何 hello 瀏覽器索引標籤中的 hello 應用程式，使用 Azure AD。</span><span class="sxs-lookup"><span data-stu-id="1f45b-146">Another advantage of this approach is a user can sign out from Azure AD, using any of hello applications signed into Azure AD, running in any of hello browser tabs.</span></span> <span data-ttu-id="1f45b-147">這會導致 hello hello Azure AD 工作階段 cookie，刪除與 hello JavaScript 應用程式會自動遺失 hello 能力 toorenew 語彙基元的 hello 登出使用者。</span><span class="sxs-lookup"><span data-stu-id="1f45b-147">This results in hello deletion of hello Azure AD session cookie, and hello JavaScript application will automatically lose hello ability toorenew tokens for hello signed out user.</span></span>

## <a name="is-hello-implicit-grant-suitable-for-my-app"></a><span data-ttu-id="1f45b-148">適合 hello 隱含授與我的應用程式嗎？</span><span class="sxs-lookup"><span data-stu-id="1f45b-148">Is hello implicit grant suitable for my app?</span></span>
<span data-ttu-id="1f45b-149">hello 隱含授予有更多的風險，比其他授權的而且您需要 toopay 注意 tooare 記載完善的 hello 區域。</span><span class="sxs-lookup"><span data-stu-id="1f45b-149">hello implicit grant presents more risks than other grants, and hello areas you need toopay attention tooare well documented.</span></span> <span data-ttu-id="1f45b-150">例如，[誤用的存取權杖 tooImpersonate 資源擁有者在隱含流程][ OAuth2-Spec-Implicit-Misuse]和[OAuth 2.0 威脅模型與安全性考量][OAuth2-Threat-Model-And-Security-Implications]).</span><span class="sxs-lookup"><span data-stu-id="1f45b-150">For example, [Misuse of Access Token tooImpersonate Resource Owner in Implicit Flow][OAuth2-Spec-Implicit-Misuse] and [OAuth 2.0 Threat Model and Security Considerations][OAuth2-Threat-Model-And-Security-Implications]).</span></span> <span data-ttu-id="1f45b-151">不過，hello 高風險設定檔是主要是因為 toohello 事實，是執行遠端資源 tooa 瀏覽器所服務的使用中程式碼的 tooenable 應用程式。</span><span class="sxs-lookup"><span data-stu-id="1f45b-151">However, hello higher risk profile is largely due toohello fact that it is meant tooenable applications that execute active code, served by a remote resource tooa browser.</span></span> <span data-ttu-id="1f45b-152">如果您計劃 SPA 架構中，有任何後端元件，或是想 tooinvoke 透過 JavaScript 的 Web 應用程式開發介面，建議使用的 hello 隱含流程權杖取得。</span><span class="sxs-lookup"><span data-stu-id="1f45b-152">If you are planning an SPA architecture, have no backend components or intend tooinvoke a Web API via JavaScript, use of hello implicit flow for token acquisition is recommended.</span></span>

<span data-ttu-id="1f45b-153">如果您的應用程式是原生用戶端，hello 隱含流程不好調整。</span><span class="sxs-lookup"><span data-stu-id="1f45b-153">If your application is a native client, hello implicit flow isn’t a great fit.</span></span> <span data-ttu-id="1f45b-154">原生用戶端 hello 內容中的 hello Azure AD 工作階段 cookie 的 hello 缺乏 deprives hello 表示維護長期的工作階段的應用程式。</span><span class="sxs-lookup"><span data-stu-id="1f45b-154">hello absence of hello Azure AD session cookie in hello context of a native client deprives your application from hello means of maintaining a long lived session.</span></span> <span data-ttu-id="1f45b-155">取得新資源的存取權杖時，這表示您的應用程式會重複會提示 hello 使用者。</span><span class="sxs-lookup"><span data-stu-id="1f45b-155">Which means your application will repeatedly prompt hello user when obtaining access tokens for new resources.</span></span>

<span data-ttu-id="1f45b-156">如果您正在開發 Web 應用程式包括後端，並使用它的後端程式碼的 API，hello 隱含流程也會不適合。</span><span class="sxs-lookup"><span data-stu-id="1f45b-156">If you are developing a Web application which includes a backend, and consuming an API from its backend code, hello implicit flow is also not a good fit.</span></span> <span data-ttu-id="1f45b-157">其他授與可提供您更多權力。</span><span class="sxs-lookup"><span data-stu-id="1f45b-157">Other grants give you far more power.</span></span> <span data-ttu-id="1f45b-158">例如，hello OAuth2 用戶端認證授與提供 hello 能力 tooobtain 語彙基元，以反映 hello 權限指派 toohello 應用程式本身，但不要的 toouser 委派。</span><span class="sxs-lookup"><span data-stu-id="1f45b-158">For example, hello OAuth2 client credentials grant provides hello ability tooobtain tokens that reflect hello permissions assigned toohello application itself, as opposed toouser delegations.</span></span> <span data-ttu-id="1f45b-159">這表示 hello 用戶端 hello 能力 toomaintain 以程式設計方式存取 tooresources 即使使用者未主動參與工作階段，依此類推。</span><span class="sxs-lookup"><span data-stu-id="1f45b-159">This means hello client has hello ability toomaintain programmatic access tooresources even when a user is not actively engaged in a session, and so on.</span></span> <span data-ttu-id="1f45b-160">不只如此，這類授與也能提供較高的安全性保證。</span><span class="sxs-lookup"><span data-stu-id="1f45b-160">Not only that, but such grants give higher security guarantees.</span></span> <span data-ttu-id="1f45b-161">比方說，永遠不會傳輸透過 hello 使用者瀏覽器的存取權杖，它們不在 hello 瀏覽器歷程記錄中儲存的風險等。</span><span class="sxs-lookup"><span data-stu-id="1f45b-161">For instance, access tokens never transit through hello user browser, they don’t risk being saved in hello browser history, and so on.</span></span> <span data-ttu-id="1f45b-162">hello 用戶端應用程式在要求權杖時，也可以執行強式驗證。</span><span class="sxs-lookup"><span data-stu-id="1f45b-162">hello client application can also perform strong authentication when requesting a token.</span></span>

## <a name="next-steps"></a><span data-ttu-id="1f45b-163">後續步驟</span><span class="sxs-lookup"><span data-stu-id="1f45b-163">Next steps</span></span>
* <span data-ttu-id="1f45b-164">如需開發人員資源的完整清單，包括 hello 通訊協定和 OAuth2 授權授與流程支援的 Azure AD 中，參考資訊，請參閱 toohello [Azure AD 開發人員指南][AAD-Developers-Guide]</span><span class="sxs-lookup"><span data-stu-id="1f45b-164">For a complete list of developer resources, including reference information for hello protocols and OAuth2 authorization grant flows support by Azure AD, refer toohello [Azure AD Developer's Guide][AAD-Developers-Guide]</span></span>
* <span data-ttu-id="1f45b-165">請參閱[如何 toointegrate 應用程式與 Azure AD] [ ACOM-How-To-Integrate] hello 應用程式整合程序上的其他深度。</span><span class="sxs-lookup"><span data-stu-id="1f45b-165">See [How toointegrate an application with Azure AD][ACOM-How-To-Integrate] for additional depth on hello application integration process.</span></span>

<!--Image references-->

<!--Reference style links in use-->
[AAD-Developers-Guide]: active-directory-developers-guide.md
[ACOM-How-And-Why-Apps-Added-To-AAD]: active-directory-how-applications-are-added.md
[ACOM-How-To-Integrate]: active-directory-how-to-integrate.md
[OAuth2-Spec-Implicit-Misuse]: https://tools.ietf.org/html/rfc6749#section-10.16
[OAuth2-Threat-Model-And-Security-Implications]: https://tools.ietf.org/html/rfc6819
