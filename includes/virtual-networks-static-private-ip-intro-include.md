<span data-ttu-id="f3a31-101">您的 IaaS 虛擬機器 (VM) 和虛擬網路中的 PaaS 角色執行個體，會根據其所連線的子網路，從您指定的範圍自動接收私人 IP 位址。</span><span class="sxs-lookup"><span data-stu-id="f3a31-101">Your IaaS virtual machines (VMs) and PaaS role instances in a virtual network automatically receive a private IP address from a range that you specify, based on the subnet they are connected to.</span></span> <span data-ttu-id="f3a31-102">在受到解除委任之前，VM 和角色執行個體會保留該位址。</span><span class="sxs-lookup"><span data-stu-id="f3a31-102">That address is retained by the VMs and role instances, until they are decommissioned.</span></span> <span data-ttu-id="f3a31-103">您藉由從 PowerShell、Azure CLI 或 Azure 入口網站停止 VM 或角色執行個體來加以解除委任。</span><span class="sxs-lookup"><span data-stu-id="f3a31-103">You decommission a VM or role instance by stopping it from PowerShell, the Azure CLI, or the Azure portal.</span></span> <span data-ttu-id="f3a31-104">在這些情況下，一旦 VM 或角色執行個體再次開始，即會收到來自 Azure 基礎結構的可用 IP 位址，其可能與先前的不同。</span><span class="sxs-lookup"><span data-stu-id="f3a31-104">In those cases, once the VM or role instance starts again, it will receive an available IP address from the Azure infrastructure, which might not be the same it previously had.</span></span> <span data-ttu-id="f3a31-105">如果您從客體作業系統關閉 VM 或角色執行個體時，它會保留其擁有的 IP 位址。</span><span class="sxs-lookup"><span data-stu-id="f3a31-105">If you shut down the VM or role instance from the guest operating system, it retains the IP address it had.</span></span>  

<span data-ttu-id="f3a31-106">在某些情況下，您希望 VM 或角色執行個體具有靜態 IP 位址，例如，如果您的 VM 即將執行 DNS 或將成為網域控制站。</span><span class="sxs-lookup"><span data-stu-id="f3a31-106">In certain cases, you want a VM or role instance to have a static IP address, for example, if your VM is going to run DNS or will be a domain controller.</span></span> <span data-ttu-id="f3a31-107">您可以設定靜態的私人 IP 位址來這麼做。</span><span class="sxs-lookup"><span data-stu-id="f3a31-107">You can do so by setting a static private IP address.</span></span>

